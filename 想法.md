# 想法

自动管理qbittorrent的PT种子

## 设计原则

* **幂等性**: 所有动作重复执行不应产生副作用或重复操作; 需要"每天一次"等窗口语义的动作必须依赖状态文件去重, 不能依赖循环频率
* **保守默认**: 高风险动作(跳检、强制汇报、删除种子、覆盖限速)默认关闭或只对显式允许的范围生效
* **状态持久化**: 跨轮次状态(规则执行历史、上传量快照、脚本设置过的限速等)统一存到 `state_file`, 程序重启后继续有效
* **fail-fast**: 启动时全量校验配置(未知键、非法格式、非法HR规则), 尽早报错, 不运行到一半才发现

## 功能

利用`qbittorrent-api`实现以下功能

* 标签/分类管理
  * [x] 自动给对应的tracker添加标签, 自动删除(替换)不想要的标签, 自动删除类似(单词相同大小写不同)的标签
  * [x] 自动完全删除标签
  * HR管理
    * [x] 自动给对应的种子添加HR标签, 如tracker1 HR为3D(3天), 下载80%触发, 则将满足触发条件的种子添加HR标签或分类, 如: !!HR3D!!, 可自定义
    * [x] 自动给做种时间已满足HR要求时间+额外时间的种子添加标签或分类, 如: --HH3D--, 可自定义

* 辅种管理
  * [ ] __支持种子分组: 将指向相同文件列表的种子归为一组, 统一处理, 支持规则筛选器__
    * [ ] __警告并暂停指向相同文件列表但文件大小不同的种子__
  * [x] 自动暂停正在上传但文件不存在或文件大小不正确的种子(可能是辅种, 原种子带文件被删除了), 并添加标签: MISSING
    * [ ] __同组中一个种子状态发生变化才触发文件丢失检查, 文件丢失后同组的所有种子全部触发文件丢失动作__

* 自定义规则
  * [ ] 自定义规则: 筛选条件+动作, 规则支持"一次执行/每日一次/冷却"等去重语义. 筛选条件: 路径, 大小, 标签, 分类, trackers, 状态, HR, 日期时间, 做种时长, 上传比率/大小(今日/本周/本月), 剩余空间. 动作: 跳检(skip-checking), 校验(full-checking 异步), 限速, 暂停, 开始, 添加标签, 添加分类, 强制汇报tracker, 添加HR标签, 添加HR分类, 移动保存路径, 基础检查(filelist/custom), 总是检查首个种子 等
  * [ ] 辅种任务跳过检查并自动开始(高风险, 见"风险与约束")

* 限速
  * [ ] 根据tracker自动限速(不覆盖用户手动修改, 见"风险与约束")
  * [ ] 接入外部程序, 根据每天总流量自定义速度曲线
  * [ ] (待完善) 复杂的限速规则, 比如指定某tracker某段时间段限速, 或者根据每天流量自动限速

* 其它
  * [x] 定期检查(默认60秒): 间隔从"上一轮处理结束"开始计时, 大库(数千种子)下单轮耗时可能超过间隔, 不能叠加执行
  * [x] 根据已有种子的tracker导出yaml配置模板, 尽量保留已有配置(含注释)
  * [x] 站点单独的设置可以覆盖全局设置
  * [ ] 支持给tracker分组, 规则筛选器可以按分组过滤
  * [ ] 规则筛选器添加非逻辑

## 实现细节

* 任务队列
  * 将所有功能视为任务task, 有两个任务队列, 快速队列和慢速队列, 每个任务有下次执行时间, 队列为时间优先队列.
  * 任务一开始被加入快速队列中, 遇到特定的耗时任务时将其加入到慢速队列中, 耗时任务完成后将任务从慢速队列中移到快速队列中.
  * 一个rule就是一个任务, 任务之间互不干扰, rule可以设置多久运行一次.
  * 任务完成后根据其设置的时间和循环次数等再次加入到快速队列中.
  * 程序主循环每2秒运行一次, 运行时检查客户端种子有无增删, 对新加入的种子创建初始任务, 删除的种子删除对应任务.
  * 主循环完成种子检查后从快速和慢速队列中取出到运行时间的任务并执行. 对慢速任务考虑异步执行, 需要对慢速任务加上异步支持. 异步仅执行发送校验请求, 主循环轮询检查请求结果.

* 站点单独的设置可以覆盖全局设置
  * 读取tracker配置后使用全局设置覆盖站点没有的设置, 后续所有判断全部使用站点的设置

* 添加分类
  * 为种子添加分类时如果overwrite=false, 将上次设置的种子hash与分类保存到文件中, 如果当前的分类与之前自动设置的分类一致则忽略overwrite规则直接修改

* state文件
  * 为减少硬盘写入, state落盘改为仅程序退出时

* 标签与分类匹配时支持正则匹配, 以"regex:"开头. 支持忽略大小写, ":ignore_case"结尾.

## 配置文件

```YAML
---
config:
    interval: 60S # 检查间隔, 支持时间单位: S秒, M分, H时, D天
                  # 注意: 间隔从"上一轮处理结束"开始计时, 单轮耗时超过间隔时不叠加

    state_file: "auto-qb-state.json" # 状态持久化文件: 规则执行历史/上传量快照/脚本设置的限速记录, 必须可写
    single_instance_lock: true # 单实例锁, 防止cron+手动同时运行互相竞争(如辅种跳检重复删/加种子)
    log:
        file: "auto-qb.log" # 日志文件路径, 留空仅输出控制台; 24/7运行建议落盘
        level: INFO
        max_bytes: 10MB # 日志轮转大小

    remove_similar_tags: true # 删除种子类似(单词相同大小写不同)的标签

    # 全局自动彻底删除标签配置
    delete_tags: # 彻底删除的标签格式, 支持正则
        - "M-Team - TP:ignore_case"
        - "regex:^BTSCHOOL$"
    delete_tags_if_has_no_torrents: # 彻底删除无种子的标签格式, 支持正则
        - @tracker_tags:ignore_case # 引用全部 tracker 配置的标签, 忽略大小写

    # 全局HR设置
    hr:
        add_tag: '' # 标签格式: 为满足HR触发条件的种子添加标签, 支持变量
        add_category: '!!HR${required_seeding_time}!!' # 分类格式: 为满足HR触发条件的种子添加分类, 支持变量
        overwrite_category: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)
        add_tag_for_satisfied: '' # 标签格式: 为做种时间已满足HR要求时间+额外时间的种子添加标签
        add_category_for_satisfied: '--HR${required_seeding_time}--' # 分类格式: 为做种时间已满足HR要求时间+额外时间的种子添加分类
        overwrite_category_for_satisfied: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)

    # 跳检有风险, 全局配置暂时屏蔽, 后续由自定义规则支持
    # skip_checking_for_cross_seeding: true # 跳过辅种检查(高风险, 见"风险与约束", 仅建议对显式允许的tracker/分类生效)
    # skip_checking_auto_start: true # 跳过辅种检查时自动启动种子
    # add_skip_checking_tags: true # 添加跳过辅种标签
    # skip_checking_tag_format: "SKIP_CHECKING" # 跳过辅种标签格式

    # qBittorrent客户端
    qbittorrent:
        host: 127.0.0.1
        port: 8080
        username: <USERNAME>
        password: <PASSWORD>

    example_rules: # 规则集名称, 自定义, 以"_rules"结尾
        rule1:
            enabled: true
            interval: 60S # 执行间隔
            execute_once: never # 动作去重: never(每次条件满足都执行, 默认, 仅适合加/删标签等幂等动作),
                                # once(每种子只执行一次), daily(每种子每天最多一次, 适合校验/开始/强制汇报/限速等),
                                # hourly(每种子每小时最多一次)
            cooldown: 0S # 冷却时间, 距上次执行成功不足该时长则跳过; 覆盖execute_once的粒度, 如 execute_once: never + cooldown: 10M
            log_level: info
            conditions: # 筛选条件必须全部满足时才生效
                - path: /path/to/file # 路径, 支持正则, 正则以"regex:"开头
                - size: ">=100MiB" # 文件大小限制, 支持大小单位: B, KiB, MiB, GiB, TiB, 支持比较符: >, <, >=, <=
                - tags: # 标签, 不同标签组之间为或的关系, 支持正则和${required_seeding_time}变量
                    - tag1,tag2
                    - tag3,tag4
                - category: # 分类, 支持正则和${required_seeding_time}变量
                    - category
                - trackers: # tracker的自定义名称, 不同tracker组之间为或的关系, 支持正则
                    - tracker1
                    - tracker2
                - state:  # 不同state组之间为或的关系, 可选: checking, downloading, complete, uploading, errored, stopped
                          # 注意: 这些是语义化状态, 不是qB原始state字符串, 映射表见"规则执行语义"章节
                    - complete&uploading # 状态为完成且正在上传, 用&连接符连接
                    - stopped
                - hr: condition-met # HR筛选, 可选: condition-met(满足HR触发条件), condition-not-met(不满足触发条件), satisfied(满足要求+额外做种时长)
                - date_time:
                    day_of_month: 1-31
                    day_of_week: 1-7
                    time: 10:00-23:00
                - seedtime: <24H # 做种时长
                - upload_ratio: ">1.5" # 上传比率
                - upload_size: ">10GiB" # 总上传大小
                - upload_size_today: ">10GiB" # 今日上传大小: 基于state_file按自然日做增量统计(记录日初各种子uploaded快照),
                                              # 同一天多次运行有效; 种子被重加/客户端重启会使uploaded归零, 统计时取max(0, 增量)
                - upload_size_this_week: ">10GiB" # 本周上传大小, 类上
                - upload_size_this_month: ">10GiB" # 本月上传大小, 类上
                - freespace:
                    path: "R:\\"
                    amount: "<100GiB" # 可用空间大小
            actions: # 筛选条件满足后执行的动作, 动作顺序执行, 默认一个动作出错后续动作将不执行
                - basic_check: filelist # 可选: filelist(对比文件列表和文件大小, 宽松),
                                        # piecehashes(包含filelist对比, 且对比两个种子文件中包含的每个块的hash, 不读取实际文件, 对直接转种的种子有效, 重新制作的块大小不同的种子无效, 相对严格),
                                        # custom(自定义)
                - custom_basic_check_program_path: "/path/to/program" # 可选: 运行自定义的basic_check程序, 参数: 暂未定
                - check:
                    mode: skip-checking # 校验, 可选: skip-checking(跳检, 高风险: 不做哈希校验直接开始, 文件内容错误时会传垃圾数据,
                                        # 必须配合basic_check且文件全部存在, 见"风险与约束"), full-checking(qB全量校验, 安全)
                    auto_start: True
                    always_check_first_one: true # 对同一保存路径下"最后校验时间最久"的种子强制校验, 意图: 每个路径至少有一个种子被证明文件完好;
                                                 # 已校验的hash记录到state_file, 不会每轮重复校验同一种子
                - start: true
                - stop: true
                - ignore_next_action_error: true # 忽略下一个动作的错误继续执行后续动作, 仅对下一个动作起效, 默认不忽略错误
                - add_tags: # 添加标签
                    - tag-format1 # 标签格式, 支持${required_seeding_time}变量
                    - tag-format2
                - ignore_next_action_error: true
                - remove_tags: # 自动删除站点标签, 可以有多个, 支持正则和${required_seeding_time}变量
                    - tag-format3
                - ignore_next_action_error: true
                - add_category: # 添加分类
                    format: category-format
                    overwrite: true # 添加分类时强制覆盖已有分类(分类只能有一个)
                - remove_category: true # 自动删除站点分类
                - export_torrent:
                    path: /path/to/export/to
                    overwrite: true
                - move_to:
                    path: /path/to/move/to
                    overwrite: true
                - upload_speed_limit: 1000KiB/s
                - download_speed_limit: 1000KiB/s
            stop_following_rules_if: conditions-met # 可选: conditions-met, conditions-not-met, action-failed, all-actions-succeed, always, never
                                                    # 注意: action-failed依赖动作结果判定(skipped不算失败), 见"规则执行语义"章节
        rule2:
            # ...

    # tracker站点配置
    trackers:
        tracker1: # 自定义tracker站点名称
            domains: # 站点域名, 可以有多个, 也可单独配置
                - domain1
                - domain2
            tags: # 自动添加站点标签, 可以有多个
                - tag1
                - tag2
            remove_tags: # 自动删除站点标签, 可以有多个, 支持正则
                - tag3
            U: 1000KiB/s  # 单种上传限速, 0指无限制, 支持速度单位: B/s, KiB/s, MiB/s, GiB/s
            D: 10MiB/s  # 单种下载限速, 同上
            hr: # HR规则
                required_seeding_time: 3D # 要求做种时间, 支持时间单位: S秒, M分, H时, D天.
                required_share_ratio: 2.0 # 要求分享率, 2.0表示2倍上传, 可以稍微设置大一点以防意外
                extra_seeding_time: 12H # 额外做种时间防止意外
                condition: 80% # `@80%` 表示触发HR的下载比例, 默认80%. 也可设置触发HR的下载量绝对值, 如`10MiB`
                # 可覆盖全局设置
                add_tag: '' # 标签格式: 为满足HR触发条件的种子添加标签, 支持变量
                add_category: '!!HR${required_seeding_time}!!' # 分类格式: 为满足HR触发条件的种子添加分类, 支持变量
                overwrite_category: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)
                add_tag_for_satisfied: '' # 标签格式: 为做种时间已满足HR要求时间+额外时间的种子添加标签
                add_category_for_satisfied: '--HR${required_seeding_time}--' # 分类格式: 为做种时间已满足HR要求时间+额外时间的种子添加分类
                overwrite_category_for_satisfied: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)
            rules:
                - ignore_next_rule_error: true # 忽略错误, 覆盖规则指定的"stop_following_rules_if"
                - @example_rules # 引用规则集
                - @example_rules.rule1 # 引用具体规则
        tracker2:
            # ...
...
```

## 规则执行语义

### 状态映射表

规则条件里的 `state` 是语义化状态, 由 qB 原始 state 字符串映射而来:

| 语义状态 | 覆盖的 qB state | 说明 |
|---|---|---|
| checking | checking, checkingResumeData, checkingDL, checkingUP | 正在校验 |
| downloading | downloading, forcedDL, metaDL, forcedMetaDL | 正在下载 |
| complete | uploading, stalledUP, pausedUP, forcedUP, queuedUP, stoppedUP | 已完成下载 |
| uploading | uploading, forcedUP, stalledUP | 正在上传做种 |
| errored | missingFiles, error, unknown | 出错(含文件丢失) |
| stopped | pausedDL, pausedUP, stoppedDL, stoppedUP | 已暂停/停止 |

`complete&uploading` = `is_complete 且 is_uploading`, 即"正在做种中"。

### 动作结果与错误处理

* 每个动作返回统一结果: `success / failed / skipped`
* `failed`: qB API 返回非200或抛出异常; `skipped`: 条件不满足(如标签已存在、分类已设置), 不算失败
* `stop_following_rules_if: action-failed` 只对 `failed` 生效, `skipped` 不影响
* `ignore_next_action_error` 只对下一个动作生效, 忽略 `failed` 继续执行

### 去重与一次执行

* 规则默认 `execute_once: never`, 只适合幂等动作(加/删标签、设分类)
* 非幂等动作(校验、开始、强制汇报、限速)必须配置 `execute_once` 或 `cooldown`, 否则60s循环会在条件成立期间反复触发
* 执行历史记录在 `state_file`, 键为 `规则名 + 种子hash + 时间窗口(日/小时)`
* `daily` 窗口按自然日切换(跨天自动重置), 与 `upload_size_today` 的统计口径一致

### 内置处理步骤与规则引擎的优先级

* 内置步骤(缺文件检查 → 辅种跳检 → tracker标签 → HR)只在"种子未匹配任何启用的规则"时兜底执行
* 规则动作执行后内置步骤跳过, 避免冲突(如规则 `start` 了种子, 内置缺文件检查又 `stop` 它)
* 缺文件检查优先级最高, 对 `MISSING` 的种子任何规则都不生效, 直到文件恢复并 recheck 通过

## 风险与约束

### 辅种跳检(高风险)

* "下载量/完成量/进度为0 + 暂停"也可能命中用户"先加种子稍后下载"的正常操作, 必须限制在显式允许的tracker/分类内
* 跳检前只做"文件存在+大小一致"检查, 内容错误时会把垃圾数据标为已完成并上传 → 可能被站点处罚
* 当前"导出→删除→重加"的实现有中断窗口(崩溃即丢种子), 且本地统计(`seeding_time`/`uploaded`)清零, 影响HR-DONE判定和上传量规则
* 约束: 文件检查不通过时降级为 `torrents_recheck`; 优先考虑 `torrents_set_location` + recheck 替代删除重加

### 强制汇报(reannounce)

* 只能在状态首次变化(如刚置为HR-DONE)时汇报一次, 高频announce可能被站点视为异常行为

### 限速覆盖

* 无条件按配置set限速会覆盖用户手动修改的值
* 只允许脚本覆盖自己上次设置的值: `state_file` 记录"脚本上次设置值", 当前值≠记录值时视为用户手动修改, 跳过并打日志

### tracker 匹配

* 必须解析 hostname 后精确匹配(含端口处理), 子串包含会误匹配伪造域名(`hdfans.org` vs `fakehdfans.org`)
* 多tracker种子: 标签取所有匹配tracker的并集, 限速冲突取更严格值并打日志, 不允许只应用第一个匹配

### MISSING 恢复

* 暂停并打 `MISSING` 标签后, 文件恢复不会自动解除
* 需要: 对 MISSING 种子每轮重查文件 → 齐了则 recheck → 通过后移除标签并恢复原状态(做种/暂停)

### 大库性能

* 每轮避免对每个种子重复调用 `torrents_trackers`/`torrents_files`(缺文件检查与辅种检查合并为一次文件列表获取)
* 文件级检查降频(如每5轮一次全量缺文件扫描)
* 单轮耗时超过间隔时从轮末计时, 不叠加; 加最小睡眠下限

### 其他

* `gen_default_tag` 需处理 IP 地址(倒数第二段是数字)和单段域名, 否则生成错误标签或崩溃
* 导出配置模板不能重写整个文件(注释会丢), 应只追加新增tracker条目
* 启动期全量校验配置: 未知键、`HR` 格式、速度单位、`state` 取值, fail-fast

## 例子1: 简单配置

```YAML

```

## 例子2: 自动添加盘符标签规则

```YAML

```

## 例子3: 自动校验并开始指定盘的IYUU辅种规则

```YAML

```

# 想法

自动管理qbittorrent的PT种子

## 设计原则

* **幂等性**: 所有动作重复执行不应产生副作用或重复操作; 需要"每天一次"等窗口语义的动作必须依赖状态文件去重, 不能依赖循环频率
* **保守默认**: 高风险动作(跳检、强制汇报、删除种子、覆盖限速)默认关闭或只对显式允许的范围生效
* **状态持久化**: 跨轮次状态(规则执行历史、上传量快照、脚本设置过的限速等)统一存到 `state_file`, 程序重启后继续有效
* **fail-fast**: 启动时全量校验配置(未知键、非法格式、非法HR规则), 尽早报错, 不运行到一半才发现

## 功能

利用`qbittorrent-api`实现以下功能

* 标签/分类管理 (内置种子任务|任务队列循环或特定情况触发)
  * [x] 自动给对应tracker的种子添加标签, 自动删除(替换)不想要的标签, 自动删除类似(单词相同大小写不同)的标签
  * [x] 自动完全删除标签
  * [x] 自动添加集数标签, 如E1-5, (~~仅种子名称中不包含集数~~, 仅种子添加时触发)
  * HR管理
    * [x] 自动给对应的种子添加HR标签, 如tracker1 HR为3D(3天), 下载80%触发, 则将满足触发条件的种子添加HR标签或分类, 如: !!HR3D!!, 可自定义
    * [x] 自动给做种时间已满足HR要求时间+额外时间的种子添加标签或分类, 如: --HH3D--, 可自定义

* 辅种管理 (种子事件驱动|每main_tick循环触发)
  * [x] 支持种子分组: 将指向相同文件列表的种子归为一组, 方便统一处理, (后续可支持规则筛选器)
    * [x] 触发方式:
      * [x] 同组中种子被删除, 或种子由上传(做种)状态转为暂停状态, 或种子进入错误状态(missingFiles/error, 重校验/恢复时发现文件缺失)时触发文件丢失检查(事件驱动, 检测到即立即处理)
      * [x] 种子保存路径变化 -> 原组剩余成员与新组已有成员触发缺文件检查
    * [x] 动作:
      * [x] 文件丢失后同组的所有种子全部触发文件丢失动作, 暂定暂停并添加标签: MISSING
      * [x] 警告并暂停指向相同文件列表但文件大小不同的种子
  * [ ] (与当前设计不符, 已移除) ~~自动暂停正在上传但文件不存在或文件大小不正确的种子(可能是辅种, 原种子带文件被删除了), 并添加标签: MISSING~~

* 主动通知 (2026-09-12 已实现)
  * [x] 程序 ERROR/WARNING 日志 -> 平台原生通知(Windows=PowerShell 调 WinRT toast / Linux=notify-send / macOS=osascript, 零第三方依赖)
  * [x] 可配置: 最低级别/免打扰时段(跨午夜)/每小时上限/同键去重窗; 节流为内存态
  * [x] 不打扰繁忙: 全屏由 OS 专注助手自动静默 toast; 程序级 quiet_hours 兜底睡眠时段
  * [x] toast 来源显示 "AutoQB": 首次运行幂等注册开始菜单 AUMID 快捷方式(失败回退 PowerShell 来源)
  * [ ] 更多渠道(webhook/邮件/Telegram, channels 结构已预留)/通知交互按钮/自定义图标(当前为解释器图标)

* 自定义规则 (rule任务|自定义触发时机)
  * [ ] 自定义规则=触发时机+筛选条件+动作.
   * [ ] 触发时机:
     * [x] interval: 固定时间间隔循环一次
     * [x] on_torrent_state_changed: 种子状态发生变化时触发
     * [x] on_torrent_added/on_torrent_deleted: 种子添加/删除时
     * [ ] 支持"一次执行/每日一次/冷却"等去重语义
   * [ ] 筛选条件:
     * [x] 路径, 标签, 分类, 大小
     * [ ] trackers, 状态, HR, 日期时间, 做种时长, 上传比率/大小(今日/本周/本月), 剩余空间.
     * [ ] ...
   * [ ] 动作:
     * [x] 暂停, 开始, 添加标签, 添加分类
     * [x] 自动校验[见`校验想法`]
     * [ ] (有风险) 移动保存路径
     * [ ] (见`限速`一节) 限速
     * [ ] (有风险) 强制汇报tracker
     * [ ] ...
  * [x] (高风险) 辅种任务跳过检查并自动开始

* 限速
  * [x] 接入 Traffic Monitor, 根据每天上传/下载量设置限速曲线
  * [x] 根据tracker自动限速
  * [ ] (待完善) 复杂的限速规则, 比如指定某tracker某段时间段限速, 或者根据每天流量自动限速

* 其它
  * [x] 定期检查(默认60秒): 间隔从"上一轮处理结束"开始计时, 大库(数千种子)下单轮耗时可能超过间隔, 不能叠加执行
  * [x] 根据已有种子的tracker导出yaml配置模板, 尽量保留已有配置(含注释)
  * [x] 站点单独的设置可以覆盖全局设置
  * [x] 支持给tracker分组, 规则筛选器可以按分组过滤 (站点 groups 字段 + tracker_group 条件, 2026-09-15; 后续: 设置页 groups 下拉快捷追加 + 辅种管理页按组筛选)
  * [ ] 规则筛选器添加非逻辑
  * [ ] 与PTD-cli合作自动分析种子的HR标签 (需要可行性验证: 获取HR标签)
  * [ ] 与PTD-cli合作自动暂停站点分享率低于规定值正在下载的非免费种子(需要可行性验证: 获取free标签)
  * [ ] 重要的问题弹窗提醒
  * [ ] 弹窗提示种子是否下载过, 什么时候, 保存路径等
  * [ ] 添加任务统计, 执行了多少任务, 成功/失败/取消/失败率/平均等待时间 等, 在退出时输出

* WEB UI
  * 目标已升级(2026-09-15 起): **WEB UI 直接替代 qBittorrent 界面**; 辅种管理是起点而非终点
  * 界面现代美观好看
  * [x] 实时查看控制辅种状态, 按辅种分组显示, 比如
    * 种子XXX 下载速度 上传速度 总上传 ... [站点1状态(上传(绿色)|暂停(灰色)|...) 站点2状态 ...] (点击后展开显示各分站点详细信息) (右键菜单: 暂停整组/开始整组/删除(删除文件可选)/强制汇报)
  * [x] 添加设置功能, 支持修改全部设置, 无需编辑config.yml
  * [x] 追剧视图: 按同一部剧组织分组(剧→季→集), 多站点辅种同集归并, 缺集徽章 (2026-09-15 追剧视图计划)
  * 波次三 · 界面诉求 30 条 (2026-09-17 完成, 星图/棱镜双 UI 同步):
    * [x] 移除空值"—"显示与"不限速"文案; 空值留白
    * [x] 删除确认框调大, 移除下方分站点种子明细
    * [x] 多选删除语义改为计数("删除 N 个组"/"删除 N 个种子"/混合分列)
    * [x] 棱镜"清除筛选"改为常显低饱和(与星图一致)
    * [x] 页面全宽显示, 去除两侧预留间隔; 横向滚动不再被两侧背景遮挡
    * [x] 左侧悬浮信息栏改为底部状态栏; 移除"切换至导航栏"功能
    * [x] "已选 N 组 · M 个种子"批量段移入筛选器同一行, 消除跳动
    * [x] 全部信息栏支持拖动排序 + 右键列选择器显隐
    * [x] 右键菜单图标彩色化(含统计/日志/主题/HR 筛选器图标)
    * [x] 右键菜单弹出时强调显示触发的行
    * [x] 行文字底色改为状态底色; 数值列按总数分档着色; 移除名称前状态图标
    * [x] 做种/用户显示为 qB 口径"已连接 (总数)"
    * [x] 标签筛选器"管理标签"项 hover 背景透明修复
    * [x] Esc 逐层取消(弹窗 → 浮层 → 筛选下拉 → 右键菜单 → 清选择)
    * [x] 分组表/明细表/追剧表补全适用信息栏
    * [x] 设置页全面重构(栅格对齐/宽度放开/文案用户化/风险注记)
    * [x] 种子详情全面重构: 纯展示 + 分组图标排版, 移除上方控制按钮
    * [x] 种子详情 title 栏配色区分 + 吸顶 1-2px 缝隙修复
    * [x] 限速末档视作无限大档位(超出档位不再解除限速)
    * [x] 限速曲线预览图末档占比收敛
    * [x] 限速曲线新增 enabled 配置
    * [x] 屏蔽除顶部导航栏外的原生右键
    * [x] 分组/种子/追剧升为一级导航; 设置右移; 统计入状态栏; 日志并入设置页
    * [x] 统计窗口空值修复 + 刷新不再闪烁
    * [x] 添加种子窗口改版(链接折叠/调大/分类标签下拉可选可输入/跳过校验警告按勾选显示/"添加后开始"默认不勾)
    * [x] 星图/棱镜切换各配身份图标
    * [x] 导航栏 logo 动态化(卫星绕转, qb 字样略减)
    * [x] 限速改为 qB 式点击弹窗修改
    * [x] 添加种子支持点击选择位置 + 下拉选择已有目录
    * [x] 详细信息 peers 页报错(`torrents_peers` → `sync_torrent_peers`)
    * [ ] "0分钟"做种时长且没有 HR 要求改为不显示 (2026-09-16 新增, 待做)

* 插件系统
  * [ ] 直接支持自定义python插件, plugin

## 实现细节

* 任务队列
  * 将所有功能视为任务task, 有两个任务队列, 快速队列和慢速队列, 每个任务有下次执行时间, 队列为时间优先队列.
  * 任务一开始被加入快速队列中, 遇到特定的耗时任务时将其加入到慢速队列中, 耗时任务完成后将任务从慢速队列中移到快速队列中.
  * 一个rule就是一个任务, 任务之间互不干扰, rule可以设置多久运行一次.
  * 任务完成后根据其设置的时间和循环次数等再次加入到快速队列中.
  * 程序主循环每2秒运行一次, 运行时检查客户端种子有无增删, 对新加入的种子创建初始任务, 删除的种子删除对应任务.
  * 主循环完成种子检查后从快速和慢速队列中取出到运行时间的任务并执行. 对慢速任务考虑异步执行, 需要对慢速任务加上异步支持. 异步仅执行发送校验请求, 主循环轮询检查请求结果.

* 站点单独的设置可以覆盖全局设置
  * 读取tracker配置后使用全局设置覆盖站点没有的设置, 后续所有判断全部使用站点的设置

* 添加分类
  * 为种子添加分类时如果overwrite=false, 将上次设置的种子hash与分类保存到文件中, 如果当前的分类与之前自动设置的分类一致则忽略overwrite规则直接修改
  * ~~将所有可能添加的分类预处理保存在TrackerConfig中, 所有可能添加的分类都可以直接覆盖~~

* state文件
  * 为减少硬盘写入, state落盘改为仅程序退出时

* 种子分组
  * 当检测到新添加的种子时自动归组, 不再每轮全量遍历分组
  * 文件大小一致性仅在新增种子归组时检查(文件列表轻易不变, 不每轮检查), 不一致 -> 警告 + 整组暂停
  * 缺文件检查事件驱动: 组内种子被删除, 或种子由上传转暂停, 或种子进入错误状态(missingFiles/error, 重校验/恢复发现文件缺失的第一现场) -> 在检测到事件的同一轮立即触发磁盘扫描(无周期轮询任务), 文件丢失 -> 整组暂停 + MISSING 标签
  * 同组中两个及以上的种子同时下载 -> 暂停并发出警告
  * 同组中有已完成的种子和正在下载的种子 -> 暂停并发出警告; 已带 MISSING 标签的完成成员不计入"已完成"——已知缺文件组允许重新下载补救, 不被拦停(多个种子同时下载仍拦截)

* 托盘常驻 UI (2026-09-12 已实现, --tray)
  * [x] 系统托盘图标(orbit)+ 深色状态窗口(CustomTkinter): 状态卡片/最近日志/暂停恢复/通知热切换/开机自启
  * [x] 运行时暂停/恢复自动管理(pause_event, 暂停=完全旁观, 恢复后增量 diff 补上)
  * [x] 双开唤起: 单实例锁 + localhost IPC(ui.port), 第二实例 --tray 静默唤起已运行实例窗口
  * [x] 托管模式首连失败重试常驻(非托管保持历史行为)
  * [x] 通知开关从未配置状态热挂载(会话级); 点击系统通知唤起窗口(单实例唤起复用)
  * [ ] 窗口内配置编辑/种子列表/规则管理; Linux/macOS 真机验证

* 性能优化
  * 可以考虑将主循环拆分为多次进行, 以平滑性能占用
  * 全面性能优化

* 校验想法
  * 依赖分组功能
    * 只校验暂停中未完成的种子(已完成/活跃中的种子直接跳过, 避免已完成种子被反复校验)
    * 组内有正在下载的种子代表整组都未完成, 不进行任何校验, 包括跳检
    * filelist需要验证硬盘上的文件是否存在以及大小是否相同, 与grouping._check_missing_files功能一致, 为了安全重新检查
    * piece hash比较只能确定两个种子相同
    * 如何确定参考种子:
      * filelist: 已完成正在上传的同组种子
      * piecehashes: 已完成正在上传的同组种子且piecehashes相同
    * 种子通过校验后可以变成同组种子的参考
    1. 有参考的种子
      mode: skip-checking (风险可控) | full-checking (无风险)
      auto-start: true

    2. 无参考的种子
      mode: skip-checking (高风险但允许) | full-checking (无风险)
      auto-start: true

* 变量命名规范
  * tor: TorrentDictionary对象
  * torrent: TorrentRecord对象
  * hash: 种子hash

* 配置错误在读取配置时检查(fail-fast), 后续可以假定配置正确

## KNOWN BUGS

* 删除类似标签 全局/站点单设置 没有分清, 比如点击开启后就无法切换到全局设置了
* 删除种子(同时删除文件)会引起缺文件WARNING
* 辅种界面添加所有适合的字段, 分组列主要展示种子的共同信息, 详细列主要展示种子的不同信息, 比如分组列可以添加进度, 取进度最高的, 先分析适合添加哪些字段
* HR在线核实插件配置需要简化, 尽量做到自动
* 设置页部分设置项显示"[object Object]"
* () HR在线核实在界面上应该有体现, 优先级: 在线信息.考察中 > 在线信息.已达标 > 在线信息.未达标 > 本地信息
* (已分析) full-checking校验时误触发WARNING: "2026-09-25 06:25:10,218 - WARNING - 规则[skip_checking_rules.r_rule] '种子名' [HDFans] (0b23c9d4) | 校验未通过(第1次, progress=0.0)"
* (已分析) full-checking有概率不触发, 一批辅种同时加入时, 部分种子成功触发full-checking, 部分没有触发(无相关log), 手动强制校验成功. 重启程序依然不触发, 怀疑是同组种子校验失败(误判)且文件映射一致(同一物理数据), 不再校验冷却相关造成的, 待调查.
* WEBUI添加新站点热重载后不会触发种子的内置维护任务
* 将webui已启动log"WEB UI 已启动: http://127.0.0.1:38080"改为localhost, 使用127.0.0.1地址可能触发浏览器自动删除localStorage的问题(BUG?), 同时查看哪些地方还提示使用127.0.0.1一并修改
* webui与扩展需要HR在线核实详情表, 将冗长的log总结成表格的形式, 扩展log需要收起来, 仅排障时使用, 一目了然, 先计划两张表要展示哪些信息

* (已修复) 辅种界面切换到种子界面再切回辅种界面时展开的分组会收起来
* (已修复) 种子详细信息窗口对部分种子会消失, 表现为: 双击种子窗口出现后几百毫秒自动消失, 部分种子正常
* (已修复) 设置页站点设置中"从已有规则集/规则中选择"按钮过高
* (已修复) 设置页中的"启用"按钮稍微调小一点, 同时"未启用"状态色与"启用"状态色相近, 不能一眼分辨
* (已修复) 设置页刷新会回到种子页
* (已修复) "The.Cat.and.the.Dragon.S01.1080p.friDay.WEB-DL.AAC2.0.H.264-MWeb" 搜索词"cat and" 不匹配
* (已修复) WEBUI右键二级菜单图标在鼠标hover在菜单上时变灰, 同时二级菜单在鼠标移出时不会消失
* (已修复) 将鼠标右键复制二级菜单移入更多操作
* (已修复) 新版设置中运行日志按等级查看失败, 选"WARNING"级显示"日志文件暂无内容"
* (已修复) 新版设置中第一页是"连接qBittorrent", 实际包含了常规设置, 将"连接qBittorrent"改为"常规"
* (已修复) 多选右键菜单应该对所有选择的种子生效
* (未计划) --tray模式下ctrl+c无法关闭
* (已修复) WEBUI添加种子时显示添加失败, 桌面弹窗WARNING, 但实际添加成功, 另外"添加后开始"选项也未生效
* (已修复) 选择种子/组后会出现"已选 0 组 · 1 个种子 开始 暂停 ..."栏高度不匹配, 导致导航栏跳动
* (已定位·非代码bug) 浏览器重启后localStorage重置 —— 真因是浏览器**站点级**「关闭窗口时清除 Cookie 和站点数据」(Edge/Chrome 里 127.0.0.1 各有一条 cookie 例外, setting=4/SESSION_ONLY): 关浏览器时该 host 所有端口的 localStorage 一起被清, 与应用无关; 已删掉 Edge 那条例外(Chrome 那条还在), 并把这条通道写进 WebUI 空存储提示与知识库(2026-09-24)
* (已修复) 历史流量显示卡顿
* (未解决) 当前任务栏图标为python图标 —— 五轮尝试均无效(详见 memory-bank/modules.md 图标结论): BMP 帧 ico + AUMID(注册表/lnk)+ 纯 iconbitmap 等组合在作者环境仍 python; 窗口标题栏/托盘图标已正确(orbit), 仅任务栏按钮未解决, 待定位
* (已修复) 修改配置保存并热重载后报错
* (已修复) 当前下载完成的种子重新校验发现文件缺失时同组不会暂停, 重新下载时会触发同组文件同时存在已完成和正在下载的冲突导致下载暂停 —— 缺文件扫描触发路径补全"种子进入错误状态(重校验发现缺失)"; 下载冲突中已带 MISSING 标签的完成成员不计入"已完成", 重新下载不被拦停; 缺文件扫描代表种放宽为已完成或错误状态成员(全组同时出错也能扫描)
* (已修复) 当前当HR触发条件为下载量时, 对于小于触发量的种子, 下载完成不会视为触发 —— 增加完全下载即触发边界(is_fully_downloaded); 兜底语义两轮修正后定为: 下载量 >= 种子大小(完整下载完)即触发 —— downloaded=0 的纯辅种(添加时数据已完整)与部分下载(如仅 1B)均不触发
* (已修复) 当前当qb关闭或崩溃时, 会输出大量重复无法连接的log
* (已修复) 当前同组的full-checking会一起触发, 应该等待同组其它种子校验完成, 根据结果处理
* (已修复) 新加的种子无法触发skip-checking
* (已修复) "[素描总动员].Sketch.2024.2160p.HamiVideo.WEB-DL.H264.DD5.1-UBWEB.mkv" 会被打上zE1标签, 暂时禁用bare number匹配
* (已修复) "[情圣3].Love.is.Hard.2025.2160p.WEB-DL.H265.AAC-UBWEB.mp4" 会被打上zE3标签, 同上
* (已修复) 当前配置中开启通知, UI显示未开启 —— Switch 初始态晚于 handler 挂载(时序), 轮询按 handler 实际状态补正
* (已修复) 当前移到组内种子至其它地方会触发两次缺文件检测 —— 移动同轮同时命中状态转移+路径变化两个触发源, _check_missing_files 增组 key 轮内去重(跨轮不抑制)
* (已修复) 断开连接后不会恢复显示已连接 —— connect() 仅启动时调用一次, 恢复只能在 tick 成功后翻转 _last_conn_ok
* (已修复) 断开连接时会输出大量重试信息

* WEBUI
    * 辅种管理
        * 再次调大删除种子确认框, 移除删除确认框中下方的分站点种子详细信息
        * 多选时"删除整组"应改为"删除??个组", 多选种子时改为"删除??个种子", 修改逻辑以符合对应语义
        * 为分组表/明细表增加所有适用的信息栏
    * 其它
        * 限速档位默认将最后一档当作无限大档位, 以防出现超过档位反而解除限速的情况, 同时调整设置中的限速曲线预览图, 减小最后一档显示比例, 当前预览图最后一档占位超80%
        * 为限速曲线添加折叠, 默认折叠, 比如第一条曲线, 第二条曲线

* WEBUI
    * [x] 种子详细窗口中的内容页中, 将目录与文件添加合适的颜色, 颜色需不同 — R10-15: 目录 = `--fg-soft`+加粗, 文件 = `--fg-muted`
    * [x] 目前"打开目标文件夹"资源管理器有概率不弹出至顶层
    * [x] 所有弹窗对超长种子名/超长url等超长文本的处理: 截断, 加tooltip和可复制按钮 — R10-13: 值行统一"单行省略 + title 全文 + 复制按钮"(wide 行保留 3 行块行)

* WEBUI
    * [x] 导航栏追剧/添加种子图标无颜色 — R11: 挂 `ico-tv`/`ico-add` 语义类(两套 UI 同源)
    * [x] 状态栏空间剩余/限制速度图标无颜色 — R11: `.ico-disk` → indigo, `.sb-limit .ico` → `--limit-hit`
    * [x] 移除状态栏"历史"文字, 仅保留图标 — R11: 两 UI 去文本 + 图标改 today-up 色(否则只剩灰点)
    * [x] 辅种页明细表无法点击排序 — R11: 独立 `detailSortKey/Dir` + 表头三态排序(14 列可排)
    * [x] 辅种表加保存路径, 明细表取消保存路径 — R11: 列模型迁移(组级取首成员值)
    * [x] 会出现两个横向滚动条 — R11: 表头撑页(clip) + `.detail` 自成滚动(去 inner overflow) + fr 逐列取整(行/表头改定宽 100%)
    * [x] 分类/标签颜色暂无意义, 改为状态色 — R11: 芯片跟随行状态语义色, HR 标签保留自身语义色

* WEBUI
    * [x] 种子页筛选器无数据
    * 辅种组暂定整组后颜色变灰,变绿,再变灰

    * 添加tracker error/warning状态, 同时可设置忽略错误或警告

## TODO

* 键盘快捷键
  * 浏览种子详细信息时支持键盘上下键切换种子
* 现场重建, 防止意外如硬盘损坏/qb损坏后难以重建的问题
* (可行性存疑) 基于搜索的辅种方式
* passkey脱敏, 将其作为一个可配置项 [计划](memory-bank/plans/26-09-22-1801-tracker-url-source-sanitize-plan.html)
* 新版设置中站点页支持查找站点
* 实验规则是否支持: 禁止带"IYUU辅种"分类的种子开始下载
* 整组迁移. 目前qb一次设定多个种子位置时会依次移动, 同组的多个种子随机丢失进度, 需要解决这个问题
* 添加追剧视图: 按同一部剧组织分组 (如何判定是否是同一部剧需要调研和头脑风暴), 分组中按第几季第几集排序, 多站点辅种不分开显示, 类似辅种视图
* 拆分大文件
* 种子未变动时不触发内置维护任务
* 版本管理
* 窗口日志显示可选择等级
* 规则筛选器添加非逻辑
* 对命中的限速曲线强调显示, 限速时可选择配置弹窗提醒
* 重新梳理ignore_next_action_error/stop_following_rules_if
* 插件系统
* ?? 暂时移除不成熟的限速设计, 包括: upload_size/upload_size_today/upload_size_this_week/upload_size_this_month, 或可改为global_upload_size/...; 包括: 记录单种上传下载量(begin_round)

* (已完成) 拖拽添加种子
* (已完成) 自定义规则触发时机: on_torrent_state_changed/on_torrent_added/on_torrent_deleted
* (已完成) 统一正则匹配与忽略大小写匹配

# 想法

自动管理qbittorrent的PT种子

## 设计原则

* **幂等性**: 所有动作重复执行不应产生副作用或重复操作; 需要"每天一次"等窗口语义的动作必须依赖状态文件去重, 不能依赖循环频率
* **保守默认**: 高风险动作(跳检、强制汇报、删除种子、覆盖限速)默认关闭或只对显式允许的范围生效
* **状态持久化**: 跨轮次状态(规则执行历史、上传量快照、脚本设置过的限速等)统一存到 `state_file`, 程序重启后继续有效
* **fail-fast**: 启动时全量校验配置(未知键、非法格式、非法HR规则), 尽早报错, 不运行到一半才发现

## 功能

利用`qbittorrent-api`实现以下功能

* 标签/分类管理 (内置种子任务|任务队列循环或特定情况触发)
  * [x] 自动给对应tracker的种子添加标签, 自动删除(替换)不想要的标签, 自动删除类似(单词相同大小写不同)的标签
  * [x] 自动完全删除标签
  * [x] 自动添加集数标签, 如E1-5, (~~仅种子名称中不包含集数~~, 仅种子添加时触发)
  * HR管理
    * [x] 自动给对应的种子添加HR标签, 如tracker1 HR为3D(3天), 下载80%触发, 则将满足触发条件的种子添加HR标签或分类, 如: !!HR3D!!, 可自定义
    * [x] 自动给做种时间已满足HR要求时间+额外时间的种子添加标签或分类, 如: --HH3D--, 可自定义

* 辅种管理 (种子事件驱动|每main_tick循环触发)
  * [x] 支持种子分组: 将指向相同文件列表的种子归为一组, 方便统一处理, (后续可支持规则筛选器)
    * [x] 触发方式:
      * [x] 同组中种子被删除, 或种子由上传(做种)状态转为暂停状态时触发文件丢失检查(事件驱动, 检测到即立即处理)
      * [x] 种子保存路径变化 -> 原组剩余成员与新组已有成员触发缺文件检查
    * [x] 动作:
      * [x] 文件丢失后同组的所有种子全部触发文件丢失动作, 暂定暂停并添加标签: MISSING
      * [x] 警告并暂停指向相同文件列表但文件大小不同的种子
  * [ ] (与当前设计不符, 已移除) ~~自动暂停正在上传但文件不存在或文件大小不正确的种子(可能是辅种, 原种子带文件被删除了), 并添加标签: MISSING~~

* 自定义规则 (rule任务|自定义触发时机)
  * [ ] 自定义规则=触发时机+筛选条件+动作.
   * [ ] 触发时机:
     * [x] interval: 固定时间间隔循环一次
     * [ ] on_torrent_state_changed: 种子状态发生变化时触发
     * [ ] on_torrent_added/on_torrent_deleted: 种子添加/删除时
     * [ ] 支持"一次执行/每日一次/冷却"等去重语义
   * [ ] 筛选条件:
     * [ ] (已实现但未详细测试) 路径, 标签, 分类
     * [ ] 大小, trackers, 状态, HR, 日期时间, 做种时长, 上传比率/大小(今日/本周/本月), 剩余空间.
     * [ ] ...
   * [ ] 动作:
     * [x] 暂停, 开始, 添加标签, 添加分类
     * [ ] __基础检查(filelist/piecehashes/custom)__, __跳检(skip-checking), 校验(full-checking 异步), 总是检查同组的首个种子__
     * [ ] 移动保存路径
     * [ ] (见`限速`一节) 限速
     * [ ] (有风险) 强制汇报tracker
     * [ ] ...
  * [ ] (高风险, 见"风险与约束") 辅种任务跳过检查并自动开始

* 限速
  * [ ] 根据tracker自动限速(不覆盖用户手动修改, 见"风险与约束")
  * [ ] 接入外部程序, 根据每天总流量自定义速度曲线
  * [ ] (待完善) 复杂的限速规则, 比如指定某tracker某段时间段限速, 或者根据每天流量自动限速

* 其它
  * [x] 定期检查(默认60秒): 间隔从"上一轮处理结束"开始计时, 大库(数千种子)下单轮耗时可能超过间隔, 不能叠加执行
  * [x] 根据已有种子的tracker导出yaml配置模板, 尽量保留已有配置(含注释)
  * [x] 站点单独的设置可以覆盖全局设置
  * [ ] 支持给tracker分组, 规则筛选器可以按分组过滤
  * [ ] 规则筛选器添加非逻辑
  * [ ] 与PTD-cli合作自动分析种子的HR标签 (需要可行性验证: 获取HR标签)
  * [ ] 与PTD-cli合作自动暂停站点分享率低于规定值正在下载的非免费种子(需要可行性验证: 获取free标签)

* 插件系统
  * [ ] 直接支持自定义python插件, plugin

## 实现细节

* 任务队列
  * 将所有功能视为任务task, 有两个任务队列, 快速队列和慢速队列, 每个任务有下次执行时间, 队列为时间优先队列.
  * 任务一开始被加入快速队列中, 遇到特定的耗时任务时将其加入到慢速队列中, 耗时任务完成后将任务从慢速队列中移到快速队列中.
  * 一个rule就是一个任务, 任务之间互不干扰, rule可以设置多久运行一次.
  * 任务完成后根据其设置的时间和循环次数等再次加入到快速队列中.
  * 程序主循环每2秒运行一次, 运行时检查客户端种子有无增删, 对新加入的种子创建初始任务, 删除的种子删除对应任务.
  * 主循环完成种子检查后从快速和慢速队列中取出到运行时间的任务并执行. 对慢速任务考虑异步执行, 需要对慢速任务加上异步支持. 异步仅执行发送校验请求, 主循环轮询检查请求结果.

* 站点单独的设置可以覆盖全局设置
  * 读取tracker配置后使用全局设置覆盖站点没有的设置, 后续所有判断全部使用站点的设置

* 添加分类
  * 为种子添加分类时如果overwrite=false, 将上次设置的种子hash与分类保存到文件中, 如果当前的分类与之前自动设置的分类一致则忽略overwrite规则直接修改
  * ~~将所有可能添加的分类预处理保存在TrackerConfig中, 所有可能添加的分类都可以直接覆盖~~

* state文件
  * 为减少硬盘写入, state落盘改为仅程序退出时

* 种子分组
  * 当检测到新添加的种子时自动归组, 不再每轮全量遍历分组
  * 文件大小一致性仅在新增种子归组时检查(文件列表轻易不变, 不每轮检查), 不一致 -> 警告 + 整组暂停
  * 缺文件检查事件驱动: 组内种子被删除, 或种子由上传转暂停 -> 在检测到事件的同一轮立即触发磁盘扫描(无周期轮询任务), 文件丢失 -> 整组暂停 + MISSING 标签
  * 同组中两个及以上的种子同时下载 -> 暂停并发出警告
  * 同组中有已完成的种子和正在下载的种子 -> 暂停并发出警告

* 性能优化
  * 可以考虑将主循环拆分为多次进行, 以平滑性能占用
  * 全面性能优化

* 校验想法
  * 依赖分组功能
    * 只校验暂停中未完成的种子(已完成/活跃中的种子直接跳过, 避免已完成种子被反复校验)
    * 组内有正在下载的种子代表整组都未完成, 不进行任何校验, 包括跳检
    * filelist需要验证硬盘上的文件是否存在以及大小是否相同, 与grouping._check_missing_files功能一致, 为了安全重新检查
    * piece hash比较只能确定两个种子相同
    * 如何确定参考种子:
      * filelist: 已完成正在上传的同组种子
      * piecehashes: 已完成正在上传的同组种子且piecehashes相同
    * 种子通过校验后可以变成同组种子的参考
    1. 有参考的种子
      mode: skip-checking (风险可控) | full-checking (无风险)
      auto-start: true

    2. 无参考的种子
      mode: skip-checking (高风险但允许) | full-checking (无风险)
      auto-start: true

## 配置文件

### 注意事项

* 标签/分类/路径 匹配时支持正则匹配, 以"regex:"开头. 支持忽略大小写, ":ignore_case"结尾.
  * 注意windows路径的"\"同样用于正则转义, 所以请使用"/"作为路径分隔符. 路径匹配默认区分大小写.

* 下载速度格式全部以B/s或[KMG]iB/s为单位, 文件大小统一以B或[KMGT]iB为单位, 不区分大小写
* 时间单位统一为S秒, M分, H时, D天, 不区大小写.
* 路径格式统一使用"/"作为分隔符.

```YAML
---
config:
    main_tick: 2S # 主循环时间间隔
    max_tasks_per_tick: 20 # 每个循环最大执行任务数

    interval: 60S # 内置任务检查间隔, 支持时间单位: S秒, M分, H时, D天
                  # 注意: 间隔从"上一轮处理结束"开始计时, 单轮耗时超过间隔时不叠加

    state_file: "auto-qb-state.json" # 状态持久化文件: 规则执行历史/上传量快照/脚本设置的限速记录, 必须可写
    single_instance_lock: true # 单实例锁, 防止cron+手动同时运行互相竞争(如辅种跳检重复删/加种子)

    log:
        file: "logs/auto-qb.log" # 日志文件路径, 留空仅输出控制台; 24/7运行建议落盘
        level: INFO         # 日志等级
        max_bytes: 10MiB    # 日志轮转大小
        format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s" # 日志格式

    remove_similar_tags: true # 删除种子类似(单词相同大小写不同)的标签

    # 自动添加集数标签: 种子添加时触发, 名称已含集数标记(S01E01/EP01/第1集等)则跳过;
    # 否则从文件列表解析集数(如 01.mkv~05.mkv -> E1-5)添加标签
    add_episode_tags: true

    # 种子分组管理(辅种管理): 将指向相同文件列表的种子归为一组, 统一检查
    #  - 组内文件大小不一致 -> 警告 + 整组暂停
    #  - 组内任一种子状态变化 -> 触发缺文件检查, 文件丢失 -> 整组暂停 + 添加标签
    #  - 种子保存路径变化 -> 原组剩余成员与新组已有成员触发缺文件检查
    # 缺文件检查统一由分组事件驱动承担(逐种子 check_missing_files 检查已移除)
    grouping:
        enabled: true # 启用种子分组管理
        missing_tag: MISSING # 文件丢失时整组添加的标签

    # 全局自动彻底删除标签配置
    delete_tags: # 彻底删除的标签格式, 支持正则
        - "M-Team - TP:ignore_case"
        - "regex:^BTSCHOOL$"
    delete_tags_if_has_no_torrents: # 彻底删除无种子的标签格式, 支持正则
        - @tracker_tags:ignore_case # 引用全部 tracker 配置的标签, 忽略大小写

    # 全局HR设置
    hr:
        add_tag: '' # 标签格式: 为满足HR触发条件的种子添加标签, 支持变量
        add_category: '!!HR${required_seeding_time}!!' # 分类格式: 为满足HR触发条件的种子添加分类, 支持变量
        overwrite_category: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)
        add_tag_for_satisfied: '' # 标签格式: 为做种时间已满足HR要求时间+额外时间的种子添加标签
        add_category_for_satisfied: '--HR${required_seeding_time}--' # 分类格式: 为做种时间已满足HR要求时间+额外时间的种子添加分类
        overwrite_category_for_satisfied: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)

    # qBittorrent客户端
    qbittorrent:
        host: 127.0.0.1
        port: 8080
        username: <USERNAME>
        password: <PASSWORD>

    example_rules: # 规则集名称, 自定义, 以"_rules"结尾
        rule1:
            enabled: true
            trigger: interval # 触发时机, 可选: "interval": 固定时间间隔循环一次, "on_torrent_state_changed": 种子状态发生变化时触发,
                              # "on_torrent_added": 新种子添加时, "on_torrent_deleted": 种子被删除时, 等
            interval: 60S # 执行间隔
            execute_once: never # 动作去重: never(每次条件满足都执行, 默认, 仅适合加/删标签等幂等动作),
                                # once(每种子只执行一次), daily(每种子每天最多一次, 适合校验/开始/强制汇报/限速等),
                                # hourly(每种子每小时最多一次)
            cooldown: 0S # 冷却时间, 距上次执行成功不足该时长则跳过; 覆盖execute_once的粒度, 如 execute_once: never + cooldown: 10M
            log_level: info
            conditions: # 筛选条件必须全部满足时才生效
                - path: /path/to/file # 路径, 支持正则, 请使用"/"作为路径分隔符
                - size: ">=100MiB" # 文件大小限制, 支持大小单位: B, KiB, MiB, GiB, TiB, 支持比较符: >, <, >=, <=
                - tags: # 标签, 不同标签组之间为或的关系, 支持正则和${required_seeding_time}变量
                    - tag1,tag2
                    - tag3,tag4
                - category: # 分类, 支持正则和${required_seeding_time}变量
                    - category
                - trackers: # tracker的自定义名称, 不同tracker组之间为或的关系, 支持正则
                    - tracker1
                    - tracker2
                - state:  # 不同state组之间为或的关系, 可选: checking, downloading, complete, uploading, errored, stopped
                          # 注意: 这些是语义化状态, 不是qB原始state字符串, 映射表见"规则执行语义"章节
                    - complete&uploading # 状态为完成且正在上传, 用&连接符连接
                    - stopped
                - hr: condition-met # HR筛选, 可选: condition-met(满足HR触发条件), condition-not-met(不满足触发条件), satisfied(满足要求+额外做种时长)
                - date_time:
                    day_of_month: 1-31
                    day_of_week: 1-7
                    time: 10:00-23:00
                - seedtime: <24H # 做种时长
                - upload_ratio: ">1.5" # 上传比率
                - upload_size: ">10GiB" # 总上传大小
                - upload_size_today: ">10GiB" # 今日上传大小: 基于state_file按自然日做增量统计(记录日初各种子uploaded快照),
                                              # 同一天多次运行有效; 种子被重加/客户端重启会使uploaded归零, 统计时取max(0, 增量)
                - upload_size_this_week: ">10GiB" # 本周上传大小, 类上
                - upload_size_this_month: ">10GiB" # 本月上传大小, 类上
                - freespace:
                    path: "R:/"
                    amount: "<100GiB" # 可用空间大小
                - previous_state: "is_uploading" # 种子之前状态, 仅on_torrent_state_changes
                - current_state: "is_downloading" # 种子当前状态, 仅on_torrent_state_changes
            actions: # 筛选条件满足后执行的动作, 动作顺序执行, 默认一个动作出错后续动作将不执行
                - checking:
                    basic_check: filelist # 用以确定参考种子, 可选:
                                          # filelist: 对比文件列表和文件大小, 宽松.
                                          # piecehashes: 包含filelist对比, 且对比两个种子文件中包含的每个块的hash, 不读取实际文件,
                                          #   对直接转种的种子有效, 重新制作的块大小不同的种子无效, 相对严格.
                                          # custom: 自定义
                    custom_basic_check_program_path: "/path/to/program" # 可选: 运行自定义的basic_check程序, 参数: 暂未定
                    with_reference: # 有参考种子
                        mode: skip-checking # 校验, 可选: skip-checking: 跳检, 有参考种子(风险相对可控), full-checking: qB全量校验, 安全
                        auto_start: true # 校验成功后自动开始
                    without_reference: # 无参考种子
                        mode: full-checking # 校验, 可选: skip-checking(跳检, 高风险: 仅进行基础文件存在与文件大小对比, 不做哈希校验直接开始,
                                            # 文件内容错误时会传垃圾数据, 见"风险与约束", full-checking: qB全量校验, 安全
                        auto_start: true # 校验成功后自动开始
                - start: true
                - stop: true
                - ignore_next_action_error: true # 忽略下一个动作的错误继续执行后续动作, 仅对下一个动作起效, 默认不忽略错误
                - add_tags: # 添加标签
                    - tag-format1 # 标签格式, 支持${required_seeding_time}变量
                    - tag-format2
                - ignore_next_action_error: true
                - remove_tags: # 自动删除站点标签, 可以有多个, 支持正则和${required_seeding_time}变量
                    - tag-format3
                - ignore_next_action_error: true
                - add_category: # 添加分类
                    format: category-format
                    overwrite: true # 添加分类时强制覆盖已有分类(分类只能有一个)
                - remove_category: true # 自动删除站点分类
                - export_torrent:
                    path: /path/to/export/to
                    overwrite: true
                - move_to:
                    path: /path/to/move/to
                    overwrite: true
                - upload_speed_limit: 1000KiB/s     # 上传速度，不覆盖单数值
                - download_speed_limit: 1000KiB/s   # 上传速度，不覆盖单数值
            stop_following_rules_if: conditions-met # 可选: conditions-met, conditions-not-met, action-failed, all-actions-succeed, always, never
                                                    # 注意: action-failed依赖动作结果判定(skipped不算失败), 见"规则执行语义"章节
        rule2:
            # ...

    # tracker站点配置
    trackers:
        tracker1: # 自定义tracker站点名称
            domains: # 站点域名, 可以有多个, 也可单独配置
                - domain1
                - domain2
            tags: # 自动添加站点标签, 可以有多个
                - tag1
                - tag2
            remove_tags: # 自动删除站点标签, 可以有多个, 支持正则
                - tag3
            upload_speed_limit: 1000KiB/s  # 单种上传限速, 0指无限制, 支持速度单位: B/s, KiB/s, MiB/s, GiB/s
            download_speed_limit: 10MiB/s  # 单种下载限速, 同上
            hr: # HR规则
                required_seeding_time: 3D # 要求做种时间, 支持时间单位: S秒, M分, H时, D天.
                required_share_ratio: 2.0 # 要求分享率, 2.0表示2倍上传, 可以稍微设置大一点以防意外
                extra_seeding_time: 12H # 额外做种时间防止意外
                condition: 80% # `@80%` 表示触发HR的下载比例, 默认80%. 也可设置触发HR的下载量绝对值, 如`10MiB`
                # 可覆盖全局设置
                add_tag: '' # 标签格式: 为满足HR触发条件的种子添加标签, 支持变量
                add_category: '!!HR${required_seeding_time}!!' # 分类格式: 为满足HR触发条件的种子添加分类, 支持变量
                overwrite_category: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)
                add_tag_for_satisfied: '' # 标签格式: 为做种时间已满足HR要求时间+额外时间的种子添加标签
                add_category_for_satisfied: '--HR${required_seeding_time}--' # 分类格式: 为做种时间已满足HR要求时间+额外时间的种子添加分类
                overwrite_category_for_satisfied: false # 添加HR分类时强制覆盖已有分类(分类只能有一个)
            rules:
                - ignore_next_rule_error: true # 忽略错误, 覆盖规则指定的"stop_following_rules_if"
                - @example_rules # 引用规则集
                - @example_rules.rule1 # 引用具体规则
        tracker2:
            # ...
...
```

## 规则执行语义

### 状态映射表

规则条件里的 `state` 是语义化状态, 直接用 qB `TorrentState` 枚举属性判定(`is_checking` /
`is_downloading` / `is_complete` / `is_uploading` / `is_errored` / `is_stopped`), 与 qB 官方语义一致:

| 语义状态    | 判定依据       | 覆盖的 qB state                                                                                   | 说明                     |
|-------------|----------------|---------------------------------------------------------------------------------------------------|--------------------------|
| checking    | is_checking    | checkingDL, checkingUP, checkingResumeData                                                        | 正在校验                 |
| downloading | is_downloading | downloading, forcedDL, metaDL, forcedMetaDL, checkingDL, queuedDL, stalledDL, pausedDL, stoppedDL | 下载中(含暂停/排队/校验) |
| complete    | is_complete    | uploading, stalledUP, pausedUP, forcedUP, queuedUP, stoppedUP, checkingUP                         | 已完成下载               |
| uploading   | is_uploading   | uploading, forcedUP, stalledUP, queuedUP, checkingUP                                              | 上传做种中               |
| errored     | is_errored     | missingFiles, error                                                                               | 出错(含文件丢失)         |
| stopped     | is_stopped     | pausedDL, pausedUP, stoppedDL, stoppedUP                                                          | 已暂停/停止              |

`complete&uploading` = `is_complete 且 is_uploading`, 即"正在做种中"(含 queuedUP/checkingUP)。
注意 `downloading` 含 pausedDL/stoppedDL 等暂停下载状态; 条件不支持 `!` 取反, 需排除暂停下载时
用 `stopped` 条件另行判断。

### 动作结果与错误处理

* 每个动作返回统一结果: `success / failed / skipped`
* `failed`: qB API 返回非200或抛出异常; `skipped`: 条件不满足(如标签已存在、分类已设置), 不算失败
* `stop_following_rules_if: action-failed` 只对 `failed` 生效, `skipped` 不影响
* `ignore_next_action_error` 只对下一个动作生效, 忽略 `failed` 继续执行

### 去重与一次执行

* 规则默认 `execute_once: never`, 只适合幂等动作(加/删标签、设分类)
* 非幂等动作(校验、开始、强制汇报、限速)必须配置 `execute_once` 或 `cooldown`, 否则60s循环会在条件成立期间反复触发
* 执行历史记录在 `state_file`, 键为 `规则名 + 种子hash + 时间窗口(日/小时)`
* `daily` 窗口按自然日切换(跨天自动重置), 与 `upload_size_today` 的统计口径一致

## 例子1: 简单配置

```YAML

```

## 例子2: 自动添加盘符标签规则

```YAML

```

## 例子3: 自动校验并开始指定盘的IYUU辅种规则

```YAML

```

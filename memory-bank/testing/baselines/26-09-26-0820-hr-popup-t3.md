# 1666 collected / 1665 passed + 1 skipped —— HR 悬停弹窗 T3 落码(做种时长原生 title 换进度仪表浮层)

> 摘要: 双 UI 成对落码(shared/hr.js 数据组装/调度/定位单点 + 6 处绑定换触发 + teleport body 弹窗单例 + 两套 CSS z-index 140 + 守阵就地改写); 数字为**合并工作树重测**(含并行入库的 versioning/button/settings 三线, 本线在其上 ±0 条 —— 守阵为就地改写未增删用例)
> 基线时间: 2026-09-26 08:20
> 档案: 26-09-26-webui-hr-popup

(档案 `26-09-26-webui-hr-popup`; 同步路径: stash → ff 合并远端 5 提交至 1905d6d → 施回,
baseline.md 冲突按切片化新约迁移为本切片)。

- **±0 条**: `test_frontend_hr_safety_wiring` 第②③段断言就地改写(旧 `:title="hrDurTitle(m)"` ×3 断言 →
  `@mouseenter="hrPopEnter($event, m)"` 触发 ×3 + `<teleport to="body">` 弹窗单例 ×1 + hr-pop 成对 CSS
  (.hr-pop/.hp-arrow/.hp-gauge/.hp-badge) + z-index: 140); `hrDurTitle` 删除, 新增 `hrPopData` 数据组装与
  hrPop* 调度方法(全为后端算好字段的展示映射, 前端零重算)。
- **前端零后端改动**: 落地面 = `shared/hr.js` +160 / 两套 index.html 各 +54(6 处绑定 + teleport 弹窗块) /
  atlas style.css +83 与 prism views.css +84(同值规则, 圆角各用本 UI 族)。

TOTAL **92%**(11112 语句 / 783 未覆盖 / 3692 分支 / 331 partial)。
浏览器冒烟: 8 档位内容矩阵 + 宽限/移入弹窗/滚动·ESC 关闭/上下翻转 + 五主题跟随(含 frost·golden 亮色) +
真实两页整页编译, 0 console 错误。

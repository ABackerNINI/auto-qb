# 核心模块 · 领域功能

> 行数为 2026-09-05 快照。所有路径相对 `src/auto_qb/`(除注明)。

> 摘要: 种子数据层 / 通知 / 托盘 / 自启 / WEB 后端与前端 / 曲线 / 集数 / 剧集 / 导出。
> ⚠ **2026-09-22 方案 C 目录迁移已落地**: 文内单文件路径为迁移前快照 —— 旧根平铺 8 文件+mixins/ → `core(/mixins)/`, 6 个基础设施 → `infra/`, web_runtime/web/web_ui/ui → `webui/{runtime,server,static}`/`tray/`; 映射总表见 [overview.md](overview.md)。
> 触发: torrents, notify, ui, autostart, web, static, curves, episodes, tvshows, exporter

## 核心模块

| 文件 | 行数 | 职责 | 关键内容 |
|------|------|------|----------|
| `torrents/` (包) | 816/5 文件 | 种子数据层(2026-09-15 由单文件 725 行转包): `compat.py`(qB 字段契约: `_SNAPSHOT_FIELDS`(全量 70 字段: 必需 21 + 重加升格 6 + 可选扩展 43, 2026-09-15 全量扩容, 字段定稿参照真机样例)/`RE_ADD_FIELDS`/`REQUIRED_TORRENT_FIELDS`(校验面不变)/`QbCompatError`/`missing_torrent_fields`)/`view.py`(Web 视图字段契约: `_VIEW_FIELDS`/`_VIEW_QUANTUM`/`view_field_value`/`view_dirty`)/`record.py`(`TorrentRecord`: slots 全量缓存 qB 种子数据字段(为 WebUI 后续功能供数), 可选字段缺失保持 qB 哨兵默认(-1/-2/8640000), `_raw` 仅收未声明字段(前向兼容), `to_dict()` 全字段导出; **另有 2 个非快照展示缓存槽** `tracker_error_msg`/`tracker_error_ts` —— 错误状态的具体原因, 由 `WebviewMixin.refresh_error_reasons` 预取后写入, 刻意**不进** `_SNAPSHOT_FIELDS`/`_raw`(不参与 `apply_delta` 与视图脏判定))/`store.py`(`TorrentStore` + `_SYNC_UNAVAILABLE` 降级信号)/`__init__.py`(全量重导出, 调用方零改动) |
| `notify.py` | ~250 | 主动通知(零第三方依赖) | `PlatformChannel`(win32=PowerShell 调 WinRT toast / linux=notify-send / darwin=osascript, 文本经 base64/转义无注入), `NotifyThrottle`(每小时上限+同键去重窗, 内存态), `NotifyHandler`(挂 `auto_qb` logger, min_level/quiet_hours/节流过滤, 防自环, emit 异常不外抛), `setup_notify`(未启用返回 None, 平台不支持抛 AutoQbError), `notify_fatal`(CLI 致命退出补发); Windows toast 来源显示 "AutoQB" —— 首次运行幂等注册开始菜单 AUMID 快捷方式(失败回退 PowerShell 来源) |
| `ui.py` | ~400 | 托盘常驻 UI(--tray) | `UiLogHandler`(日志环形缓冲 drain), `ShowIpcServer`/`send_show`(单实例唤起, 端口写 state_file 同目录 ui.port), `TrayUi`(CustomTkinter 深色窗口: 状态卡片/暂停恢复/通知热切换/开机自启/日志视图; pystray 托盘 6 项菜单; 100ms 事件轮询, 跨线程仅 queue+Event+只读快照), `run_tray`(入口; GUI 栈仅 tray 分支加载); 通知热开启: `setup_notify(force=True)` 从未配置状态会话级挂载(UI 开关/托盘勾选); toast 点击激活 = 以 launch_arguments 重启应用(AUMID 快捷方式 Arguments, 每次启动幂等重写)经单实例锁唤起窗口; 窗口图标 Windows 走 CTk iconbitmap(.ico, 防被 CTk 默认图标延迟覆盖) |
| `autostart.py` | ~90 | 开机自启(零依赖) | 资源: `assets/icon.png`(256, 托盘)+ `assets/icon.ico`(多尺寸, Windows 窗口图标); ⚠️ **任务栏图标修复的最终结论(四轮实测+对照诊断)**: ①ICO 必须为 BMP 帧 —— Pillow 保存默认 PNG 压缩帧, Tk 8.6 `iconbitmap` 渲染不了(静默失败回退 python 图标), 生成须显式 `bitmap_format="bmp"`; ②**进程须先设显式 AppUserModelID(`_set_windows_appid`, 首窗口前)** —— 诊断对照: A/B/C/D 四窗口全部运行在已设 AUMID 的进程内且均为 orbit, 而无 AUMID 时无论 iconbitmap/iconphoto/WM_SETICON 任务栏恒为 python.exe 图标(AUMID 才是决定性变量, 诊断未隔离该变量曾致误判回退); ③CTk 窗口上 `iconbitmap`(BMP 帧 ico, 16~256 多尺寸)+ `_iconbitmap_method_called = True` 标记配合; ⚠️ **勿叠加 `iconphoto`/ctypes `WM_SETICON`(单尺寸 32px HICON)**: 高 DPI(4K 200%)下任务栏需要 ≥48px 大图标, 单尺寸 HICON 被 explorer 弃用回退 python 图标(诊断: A/B/C=orbit, D=CTk 蓝框); 三平台: Windows HKCU Run 注册表 / Linux XDG autostart desktop / macOS LaunchAgents plist; `is_enabled`/`enable`/`disable`; 启动命令 = 解释器 + 配置绝对路径 + --tray; 失败抛 AutoQbError(UI 提示) |
| `web.py` | ~380 | WEB UI 后端(FastAPI) | `create_app`(API: status(**含 `version` 透出给顶栏**)/**state(支持 `?rid=` 版本门控: 版本一致时不回传 groups)**/groups/组控制/**config schema+树读取+树写回+只读预览**/**search 种子搜索**/**paths 已知目录聚合**/**fs/dirs 目录浏览与 fs/mkdir 新建目录(R10-11, 允许根白名单 + realpath 边界)**/**open-path(路径一律服务端派生; 单文件种子定位选中, R10-10)**, 全部 Bearer 鉴权(范围显式限定 `/api/*`, require_token 按 Request 路径放行静态与重定向); UI 重定向 `/`→307 `/atlas/` + `/newui/*`→307 `/prism/*`) + 静态挂载 web_ui/static + **非 /api 响应统一 `Cache-Control: no-cache`**(防升级后浏览器启发式缓存旧 JS/CSS, 未变仍 304)), `ensure_web_token`(随机生成持久化 data_dir/web.token), `start_web_server`(uvicorn 独立线程; 返回 `WebServerHandle`(含 `stop()`/`wait()`/`started`), 就绪或确认失败后才记日志 —— 起线程即打印会掩盖 bind 失败), `stop_web_server`(请求停止 + join 等线程退出; 热重载重启同端口必须先等, 否则 Errno 10048), `_run_server`(线程入口包一层: uvicorn 启动失败走 `sys.exit(STARTUP_FAILURE)`, SystemExit 在非主线程被 threading 静默吞掉, 就地记 ERROR 以免静默), `_app_version`(**函数内延迟导入** `__version__`: `__init__.py` 先导入 qbmanager 再赋值版本号, 顶层导入会循环导入); Web 线程只读快照+投递命令, 写操作经命令队列在主循环线程执行; 配置保存经 `config/writer.py`(校验/R 级回退/round-trip 写盘)后投递 `reload_config`; 搜索索引(hash->name+files)由主循环按需构建并原子替换, Web 线程只读, 种子名匹配即时(读 store.by_hash), 文件列表匹配依赖缓存索引(文件 API 只在主循环线程); 索引按 hash 增量维护(已建条目只刷新名称/新增补拉/已删淘汰) + 单次限流(SEARCH_INDEX_BUILD_BUDGET), 未拉完保持脏由下一 tick 续建, qB 断连时中止不写空 files |
| `web_ui/static/` | — | 前端(Vue 3 CDN 本地托管, 无构建链; **2026-09-15 双界面目录化**: `atlas/`=星图(旧·单主题) `prism/`=棱镜(新·五主题) `shared/`=共享逻辑层, 目录即 URL) | atlas/index.html(星图根模板 + **图标 sprite**(`<svg class="icon-sprite">` 内 symbol + `<use href="#i-*">`, 无外部图标库依赖; 新增图标只改 sprite **同一处**) + `ce-field` 的 x-template + **toast 容器与站内确认框**单例 DOM)/shared/app.js(顶部 **列模型 `GROUP_COLUMNS`/`DETAIL_COLUMNS`**(列头/单元格/grid 模板/列选择器的单一来源; **分类在标签之前**(分组表与明细表同序); 列宽按列 key 存 `autoqb_cols_v4` = `{widths,hidden,manual}`) + **列选择器**(位于搜索框右侧; 与筛选器弹层**同构** —— 选项挂 `.pop-item`、无勾选框、**无数字徐标**、整行点击切换, 选中靠左侧色条 + 亮字; 锁定列显示"必显"; 另带恢复默认列宽 / 适应窗口宽度) + **列宽交互**(拖拽只改被拖列 / Shift 拖与相邻列互挤 / 双击按内容自适应 / 未手动调过时随窗口实体化) + 顶栏搜索框在真实辅种组上筛选(任一成员命中即保留整组, 组行沿用真实组 key → 组级操作可用; 命中成员与组行均 `search-hit` 高亮; 未归组命中以单种子虚拟行 `u-<hash>` 兜底, 右键退化为单种子菜单) + **左侧固定信息栏**(辅种页 `.layout` 两列 grid + `.rail` 自身 `position:sticky`; 承载 辅种分组/种子总数/上行下行速度/刷新间隔 + 今日流量卡 + 限速对照卡 —— 用户要求"移到左边、固定位置、不随窗口滚动"; 吸顶偏移与最大高度用 `--head-h`, 由 app.js `_syncHeadHeight()` 把 `.sticky-head` 实测高度写进 :root(在 mounted/resize/updated 同步, 值未变则跳过), **不写死**) + **表头逐列类 `hc-<列key>`**(供单列表头对齐, 如 `hc-count` 右对齐"站数") + **状态分布条与筛选区**(前端聚合: 堆叠条 + 可点击 chip 状态筛选 `kindFilter` + 保存路径下拉 `pathFilter` + **标签/分类/站点多选弹层** `tagFilter`/`categoryFilter`/`siteFilter`, 定义见 computed `filterDefs`; 同一筛选器内为\"或\", 不同筛选器间为\"且\", 叠加搜索) + **组级派生列**(computed `decoratedGroups` 一次性计算: 状态摘要图标与配色、共同标签/分类(交集/一致值, 差异显示 `±`/多分类)、保存路径、大小一致性) + **HR 标签语义色**(`tagClass()` 按后端透出的标签文本匹配) + 分组列表 **rid 增量轮询**(带 `?rid=` 请求, `updated:false` 时只更新徽章不重建数据) /展开明细(列由 `visibleDetailCols` 驱动; **不再有\"合计\"行**)/右键菜单(**只弹菜单不展开明细**; 正操作在上、删除在最后; 图标按语义着色; **删除只有一个菜单项**, 是否连带磁盘文件由确认框勾选; **视口边界吸附** `_menuPos`, Esc/外部点击关闭); **轮询策略**: `setTimeout` 链式续排(不堆叠请求) + 页面隐藏(`document.hidden`)停轮询 + 无变化退避 2s→5s→10s + 失败退避至 15s; **交互原语**: `toast(text, kind)` 替代 `alert`、`confirmDialog`/`promptDialog`/`confirmWithOption` 返回 Promise 替代 `confirm`/`prompt`(单例, 取消/遮罩/Esc 均结算为 null/false 且**不发出任何请求**)) /shared/config_editor.js(图形化配置编辑器核心 mixin: 分组导航(带图标)/加载保存/只读预览/路径读写/列表与开关/**数值+单位下拉**(`UNIT_OPTIONS` + `cfgUnitParts`/`cfgSetUnit`)/**规则引用下拉**(`ceRefOptions`)/字段灰显与风险标识/相关设置子卡/站点与曲线专段(含阶梯图)/`cfgFlatten` 扁平化渲染项; 删除类操作同样走 `confirmDialog`) /shared/config_rules.js(规则集专段: 规则卡折叠与启用开关/条件与动作的选择面板与序号/16 条件/12 动作的插件与 spec 编辑, 动作按 trigger 过滤) /shared/config_hub.js(设置页**新版 Console Hub**(2026-09-21): 首页卡片总览(分段 LED 三态 + 等宽读数 + 按配置项名搜索直跳) → 二级页「块 → 行」两层级 + 行尾 `?` 就近说明浮窗(富文案表 `HUB_HELP` 优先, 否则回退 schema 的 help/risk/default); **与经典页共用同一棵 YAML 树与全部 cfg* 读写**, 只换版式; `hub-field` 组件继承 `ce-field` 全部读写、仅换 `#tpl-hub-field` 模板; 站点/规则/限速/日志四个专段各自单独渲染) /shared/console_hub.css(新版版式与控件样式, 两套 UI 共用: `hb-` 前缀防与种子表 `.row` 等既有类撞; 方角控件 + `--tone` 单开关派生描边与发光(**派生变量声明在使用 --tone 的那一层元素上**, 写进 `:root` 会被固化)、切角只留大面(块/规则卡/曲线卡/分区图标/浮窗, 控件层切角会裁掉 box-shadow)、分段 LED、就近浮窗面板+独立箭头; 颜色全走主题令牌) /atlas/style.css(**单一 `:root` 设计令牌**: 背景/前景/语义色(含筛选/图例用的 `--violet/--pink/--teal` + 分级补充色 `--indigo/--cyan/--lime`, 与 HR 标记用的 `--hr-pending`/`--hr-done`)/浮层色 `--bg-elev`/表面/圆角/阴影/动效(`--ease-out`/`--ease-spring`/`--dur-fast`/`--dur-slow`), 组件规则只引用变量(调色改一处); **微动效区**统一节奏(校验中呼吸、chip 悬浮、按钮按下回弹、浮层缓动, 全局 `prefers-reduced-motion` 降级); 组状态徽标、**HR 标签语义色**(`.tag-chip.hr-pending`/`.hr-done`)、命中高亮、风险标识、子卡、选择面板、**弹层(`.col-menu`/`.pop-menu`)/列选择器/筛选按钮**、阶梯图等均在此; 响应式断点 1180px(隐藏指标胶囊)与 900px(顶栏折行 + 侧栏/主从单列))/shared/vendor/vue.global.prod.js **2026-09-15 双界面目录化命名**: 旧版 UI 迁 `atlas/`(星图), `newui/` 改名 `prism/`(棱镜), 共享层(三件套/vendor/icon.png)收 `shared/`; web.py 加 `/`→307 `/atlas/`(默认 UI)与 `/newui/*`→307 `/prism/*` 书签兼容 —— prism/index.html(整体沿用旧模板契约: .group-head/.detail-head/gridStyle 列数/.search-hit/.ctx-menu/.sticky-head+--head-h/.bulk-bar+--bulk-h/tpl-ce-field/sprite #i-*/toast 确认框单例; 顶栏加主题切换器与新旧互切链接)+css 分层(tokens.css 结构令牌/themes/*.css 每主题全色令牌/base/components/views)+js/theme.js(html 同步置 data-theme 防 FOUC/localStorage `autoqb.ui.theme`/prefers-color-scheme 跟随/事件委托绑定不进 Vue 状态); 5 主题 ocean/galaxy/frost/golden/orbit(色板源自 .github/skills/theme-factory + 品牌定制), `/atlas/` 与 `/prism/` 由 StaticFiles(html=True) 目录即 URL 服务, 逻辑层三件套单一来源共享(只加法不改名) |
| `curves.py` | 124 | 限速曲线纯逻辑 | `parse_history_dat`(Traffic Monitor dat 解析), `aggregate`(day/month/Nd 聚合), `curve_speed`(全程分档覆盖), `merge_direction`(取最严), `normalize_period`, `bytes_to_kib`。无项目内依赖, 便于单测 |
| `episodes.py` | 128 | 集数解析 | `_EPISODE_PATTERNS`(第x集 > S01E05 > EP05 > E05 优先级), `extract_episodes_from_files`/`extract_episodes_from_names`(tvshows 文件兑底共用, 仅视频文件, 排除分辨率/年份), `format_episode_tag`(连续才加, 格式 `zE1-5`), `name_has_episode_marker` |
| `tvshows.py` | ~330 | 剧集识别(追剧视图, 2026-09-15 新增, 零第三方依赖) | `parse_release(name) → ParsedRelease(key, title, kind, season, ep_start/end, date)`: 定位剧集标记(S01E05/1x05/第x集[话]/EP05/E05/日期/动画 bare number)→ 剧名候选(标记前段+字幕组括号前缀剥离 `_strip_group_prefixes`)→ 归一化成键(`_NOISE_RE` 发布参数噪音 + `_CJK_NOISE_RE` 中字/字幕说明 + `_EXTRA_NOISE_TOKENS`; **年份/国家/续作数字为身份差分一律保留** —— 取向宁可误拆不可误并)。集键三形态 `("ep",n)/("range",a,b)/("date",iso)/("pack",)`(整季包不展开成 N 行)。多季合包(S01-S04/第1-4季)→ 未识别。`parse_files(files)`(目录段季号 + 复用 episodes 集数解析)/`refine_with_files`(季包展开集数范围、未识别从文件名拗救)。已知限制: 动画绝对编号归 S01 不跨季、纯数字尾缀不视为季、不做简繁转换。详见 memory-bank/plans/26-09-15-1534-webui-shows-view-plan.html |
| `exporter.py` | 139 | YAML 模板导出 | 收集全部 tracker 域名 → 找未配置的 → 生成条目 (默认标签=倒数第二级域名, `hd/pt` 后字母大写), `--only-missing` 最小骨架; 重读原始文件时复用 `_strip_none`(文件已被 load_config 校验) |

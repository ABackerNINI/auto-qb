# 全量测试耗时归因与优化
> 摘要: 大头是**环境**不是代码 —— 用户两次调整系统层排除项后四类文件操作全部 <1ms, 全量 **75s → 19.6s(3.8×)**、并行 `-n 4` **5.0s(15×)** 且波动消失 ⇒ 并行结论**翻转为建议采用**。等用户说「提交」并拍板是否引入 xdist
> 触发: 测试慢, 耗时, 跑全量, 性能, 优化收益, 度量, 并发跑, 并行, xdist, node --check, 临时目录泄漏, 白名单, 排除项
> 最后活动: 2026-09-23 18:11

## 状态

**代码侧(A–E)已全部处理完**; **大头在环境**, 用户已修好。整体 **75s → 19.6s**。

| 项 | 结果 |
|---|---|
| A `sidefx.report()` 单次求值 | teardown 4.71~8.97s → **1.04~1.20s** |
| B 前端 JS 语法守阵单进程批量 | 7.375s → **0.378s**; 语义与 `node --check` **8/8 一致** |
| C basetemp 钉进 TMPDIR | **撤回未实施**(只把删除从收尾挪到起始, 总量不变) |
| D 临时目录泄漏 | **3 处**(`test_web` / `test_expr_eval` / 新发现 `test_trigger_events`); 残留 2 → 1 |
| E 过期耗时画像 | `run.md` + 连带 `tmpdir.md` / `techContext.md` |

**文件操作三段状态(全部 `TMPDIR=R:/Temp/auto-qb/tests`)**:

| 操作 | ① 初始 | ② 只加白名单 | ③ 调整设置后 |
|---|---|---|---|
| `os.mkdir` | — | 0.64ms | **0.13ms** |
| 写 512B | 20.19ms | 0.58ms | **0.21ms** |
| `os.remove` | 43.02ms | 14.60ms | **0.16ms** |
| `os.rmdir` | — | 52.72ms | **0.14ms** |

**全量耗时(每条都 `1191 passed + 1 skipped`)**: 串行 **19.37~20.16s**; `-n 2` 8.56s;
**`-n 4` 5.00 / 5.06s**; `-n 8` 4.88s; `-n 16` 6.82s(更差); `-n 4`+覆盖率 7.61s(91%)。

- **沙箱假设已彻底排除**: ②③ 两轮复测**都**是 "Sandbox bypassed"(条件相同)⇒ 差别来自用户设置, 不是沙箱。
- **并行结论已翻转**: 早些时候 `-n 4` 在 15.94~209.77s 乱跳(尾部比串行还差)⇒ 当时判定"闸门保持串行";
  底层修好后同一命令稳定在 ~5s ⇒ 改为**建议 `-n 4`**。**教训: "并行划不划算"取决于底层是否串行, 底层一改就要重测。**
- 剩余最慢条目**全是真实睡眠**(1.52s / 0.91s×2 / 一串 ~0.51s), 3383 条 <5ms ⇒ 串行侧**没有明显可再挖的大项**。

## 待办(下一步从这里接)

1. **拍板是否引入并行** —— 结论已翻转成"建议采用 `-n 4`"(5.0s, 波动极小)。落地需: ①加 `pytest-xdist` dev 依赖
   ②**补控制器侧台账聚合**(否则并行跑看不到「越界 0 条」那行 —— 拦截仍在, 只是不打印)③决定闸门是否换并行
   ④覆盖率基线重刷(并行 `623 未覆盖 / 219 分支` vs 串行 `622 / 218`)。
2. **提交** —— 改动: `tests/` 4 份 + `memory-bank/`(坑档 3 份 + 基线 + 档案 + 切片 + 索引) + `techContext.md`。
   **用户尚未说「提交」**。
3. **若哪天又变慢**: 先按**操作类型逐项复测**(别只测"写文件"), 并注意白名单**粒度** ——
   只加项目子目录没用, 要按 Temp 目录加。三步定位法与探针见下面的单点指针。

## 单点指针

- 耗时成因 / 度量纪律 / **定位三步** → [../pitfalls/testing/perf-measurement.md](../pitfalls/testing/perf-measurement.md)
- 并行(含 sidefx 在 xdist 下的行为、结论翻转的判据) → [../pitfalls/testing/parallel-run.md](../pitfalls/testing/parallel-run.md)
- 完整归因与决策 → [../tasks/26-09-23-test-suite-perf.md](../tasks/26-09-23-test-suite-perf.md)
- 基线数字 → [../testing/baseline.md](../testing/baseline.md)

# git rebase/merge/stash 禁令解除 — 三处单点回写
> 摘要: 拦截层毁 `.git` 的根因已修复(用户 2026-09-25 确认), 用户明示解除 rebase/merge/stash 禁令, 范围 AGENTS.md / pitfalls / my-commit-flow。三处禁令表述全部改为"已解除 + 档案保留": AGENTS.md 3 处、pitfalls/git 4 个主题文件 + _about.md、包内 README/references/3 个脚本; kb.index 重建索引; history-integration.md 曾超 cap 6,265 → 压回 5,930。
> 触发: git, rebase, merge, stash, 禁令解除, 拦截层, my-commit-flow, pitfalls
> 最后活动: 2026-09-25 07:55

## 状态

**已完成** (随本次提交入库并推 Gitee develop):

- AGENTS.md: 「跑 git 命令前」提示、会话协议①(`stash` 被禁括注)、「⚠️ 环境硬约束」🔴禁令条 → ✅ 已解除条(高风险整合前仍建议 `cp -a .git` 备份; ff-only 首选不变)。
- pitfalls/git: `_about.md` 摘要改口径; `history-integration.md` 顶部加「✅ 状态: 禁令已解除」节 + 两条禁令节标注档案化(正文保留作事故档案与恢复手册); `editing-traps.md` stash 节改为"git archive 对照方案(stash 已可用)"、GIT_EDITOR 条与禁令脱钩; `sync-pull.md` rebase 步骤警告改为已解除注记。
- pitfalls 其他: `docs/pack-readme-entry.md` 示例换成仍成立的事实(推没推上只看 ls-remote); `kb/discipline.md` 的提及是历史事例, 保留未动。
- my-commit-flow: README 反模式 2 条 + 尾部指针; `references/pipeline.md` 禁用块改 ✅ 已解除(先同步后提交的顺序纪律不变)、删"merge 本就敏感"; `references/anti-patterns.md` 合流 3 条改写; `push.py` / `commit.py` / `preflight.py` 文案 7 处(落后 STOP 措辞、工作区行等)。
- 生成物与守卫: `commands run kb.index` 重建(类索引/顶层索引随新摘要更新); `test_kb_files_respect_caps` 一度红(history-integration.md 6,265 > 6,000), 压缩状态节与一条复发记录后回绿(5,930)。

## 实测数字

- `test.pkg` 47 passed; `test.full` 2 failed(均为基线已记载的 GBK 假红) + 1,593 passed + 1 skipped / TOTAL 91% / 18.5s —— 已记入 `testing/baseline.md` 耗时台账。
- `doc.caps` PASS(AGENTS.md 与包 README 均在上限内)。

## 正在进行 / 待用户决定

- **范围外残留** (用户范围只给了 AGENTS.md / pitfalls / my-commit-flow, 以下同事实未回写, 待授权):
  - `memory-bank/conventions/collaboration.md` L12"(禁 rebase)"、L50-51 引用"rebase 会毁 .git"条;
  - `.agents/skills/memory-bank/SKILL.md` L14 同步步骤仍写"rebase/stash 一律禁用";
  - `memory-bank/conventions/process.md` L21 旧 `git pull --rebase` 口径(陈旧早于本次)。

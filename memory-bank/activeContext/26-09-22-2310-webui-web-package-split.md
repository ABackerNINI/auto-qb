# web.py → web/ 包拆分
> 摘要: 零行为变更纯结构重构已入库；剩三条安全发现可在 auth/events/system 局部落刀
> 触发: web 包拆分, create_app, Router, 安全发现
> 最后活动: 2026-09-22 23:10

## 状态

零行为变更纯结构重构，已随提交入库。守阵 2 条红验（金清单 60 条 / 组装壳 ≤150）；
全量 1188 passed + 1 skipped；冒烟 70×2 全绿。

**剩余**：三条安全发现 S1-01 / S1-02 / S1-05 可在 `auth.py` / `events.py` / `system.py` 局部落刀
（上游已修 S1-05 脱敏，见 `44c1a0f`）。push 后真机无需走查（行为零变更，冒烟已覆盖）。

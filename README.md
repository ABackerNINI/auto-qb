<!--
  ✍️ 写作约定(2026-09-22): 本 README 从用户价值出发, 只讲「你能得到什么」, 精简干练;
  不堆实现细节 —— UI 交互口径、内部机制、配置键名、工程数字一概不进正文, 权威记录在
  memory-bank/ 与 docs/configuration.md, 需要时链接过去。新增 / 修改功能描述前先读这条, 别把细节堆回来。
-->

<div align="center">

# auto-qb

**基于 qBittorrent WebUI API 的 PT 种子自动化管理工具**

[![Python](https://img.shields.io/badge/python-3.12+-blue?logo=python&logoColor=white)](https://www.python.org/) [![License](https://img.shields.io/badge/license-Apache%202.0-green)](https://www.apache.org/licenses/LICENSE-2.0)
[![Status](https://img.shields.io/badge/status-alpha%20%F0%9F%9A%A7-orange)]() [![CI](https://img.shields.io/github/actions/workflow/status/ABackerNINI/auto-qb/ci.yml?branch=develop&label=CI)](https://github.com/ABackerNINI/auto-qb/actions/workflows/ci.yml)

Web UI 管理台 · 标签 / 分类 / HR 管理 · 辅种分组与缺文件保护 · 自定义规则引擎 · 多级限速 · 校验 / 跳检 · 桌面通知 · 托盘常驻

</div>

---

auto-qb 常驻后台运行,实时跟随 qBittorrent 的状态,自动帮你打理 PT 种子:该打的标签自动打、HR 帮你盯着、有风险的操作先拦住。它与 qB 之间只同步发生变化的数据,几乎不给 qB 增加负担。

> 🚧 **施工中**:功能已实现、测试完备,但尚未经过大规模实机验证。标记 🚧 的功能请先用 `--dry-run` 观察,谨慎在生产环境使用。

## 目录

- [功能特性](#功能特性)
- [快速开始](#快速开始)
- [命令行参数](#命令行参数)
- [托盘模式](#托盘模式)
- [桌面通知](#桌面通知)
- [配置参考](docs/configuration.md)
- [设计理念](#设计理念)
- [开发测试](#开发测试)
- [免责声明与许可证](#免责声明与许可证)

## 功能特性

### 🏷️ 标签 / 分类管理

- 按 tracker 自动添加 / 删除站点标签,自动清理相似标签(单词相同、大小写不同)
- 不再需要的标签可自动清理:能彻底删除,也能只在无种子使用时才删
- 新种子自动打**集数标签**:从文件列表解析集数,如 `EP01.mkv`~`EP05.mkv` → `zE1-5`;多集不连续则视为不可靠、放弃打标

### ⏱️ HR 管理

- 满足 HR 触发条件(下载比例 / 下载量)时自动加 HR 标签或分类,如 `!!HR3D!!`
- 做种时长达到「要求时间 + 额外时间」后自动加达标标签 / 分类,如 `--HR3D--`
- 每个站点可单独配置 HR 规则,覆盖全局设置

### 🔍 HR 在线核实(部分种子 HR 站点)🚧

有些站点只有**一部分**种子受 HR 约束,且站点不提供可识别的逐种标记 —— 只能上站查。开启后按站点
定期取「我的 H&R」清单、逐种子对账建索引,判定分**三态**: 受管束 / **已核实不受管束(安全放行)** / 未核实。

- **取数由浏览器扩展完成**, cookie 不离开浏览器 —— 后端零 cookie/passkey 依赖(手工填 cookie 已否决)
- **站点数据是权威数据**: 触发与达标都看站点侧 —— 直接治「转移种子」(A 客户端下载 → B 客户端保种,
  B 的 `downloaded=0` 会被当辅种而漏管)与多客户端场景
- **频控默认极保守**(90s 间隔 / 12 每小时 / 60 每天, 全部可配): 访问频度是账号安全的第一条防线;
  扩展侧另有一道**独立的硬上限**(访问同站 10/时·50/天, 下种 50/时·200/天)作第二道闸 ——
  后端写错也打不爆站点
- 多实例**共享站点数据**(站点分文件 + 每站点一把锁), 站点访问频率由数据有效期决定
- **节流不当故障**: 日志里 `配额/间隔受限`(被自己的频控拦下)只记一条 INFO 并按根因去重;
  启动 / 关闭这类自己决定的消息也走 INFO —— 只有页面改版、取数失败、通道静默才 WARNING
  (否则通知会被分钟级轮询淹没)
- **站点数据自带两层保护**: 每次写盘留上一版为 `.bak`, 读到坏文件时把现场挪到 `.bad-<ts>` 并尝试恢复
- 两个只读口令: `--hr-once` 走查「现在能不能取到数」/ `--hr-status` 看「已经拉到什么数据」
  (都不写盘、不加锁、不连 qB, 可与正式实例并发); 浏览器界面里也能看 —— 设置页侧栏「**HR 站点状态**」
  (每站点的数据新鲜度 / 索引进度 / 配额熔断 / 现在为什么不放行)
- **WEB UI 一眼分清「能不能删」**: 做种时长列按删除安全档位着色(不能删=橙 / 可删=青 / 未核实=灰,
  站点结论优先于本地)并带**来源徽标**(在线 / 本地 / 策略 / 未核 —— 结论来自达标优先级链哪一档),
  悬停看依据与站点侧值; 删除确认框点名仍在 HR 管束的种子; 筛选器支持按档位与来源过滤;
  批量选中时提示「含 N 个不能删」
- **四类事件带标签**: `[HR 登录失效]` / `[HR 熔断]` / `[HR 页面改版]` / `[HR 通道静默]` —— 日志里可 grep,
  文案直接给动作(去哪个浏览器登录 / 看什么)。登录失效**不计失败也不推熔断**(重试无用, 它要的是人去登录)
- **接第二个站点只改配置**: NexusPHP 形态的站点(`myhr.php` 九列表)不用写代码, 站点之间完全隔离
  (各自文件 / 各自锁 / 各自配额与熔断)

> 🚧 **当前状态(M4 已交付, 待真机走查)**: 离线管道(`src/auto_qb/hr/`, 含 `--hr-once` 只读走查与
> `--hr-status` 只读现状报告) + **浏览器扩展取数通道**(本地 `127.0.0.1` 端点 + 取数线程 + 只读视图) +
> **判定联动**(三态接进种子记录: 打标 / WebUI / `hr` 规则条件 / `tor.hr_*` 表达式四个消费点) +
> **多站点与打磨**(第二站点零代码接入 + 站点级状态展示 + 四类事件标签 + 接入指南)均已落地,
> 扩展在 [`extensions/hr-fetch-proxy/`](extensions/hr-fetch-proxy/README.md)(装一次、跑在日常浏览器里;
> 自带**运行日志**: 分级 + 条数上限可设至 10000, 选项页④区过滤查看)。
> WebUI 详情里能看到**三态与依据**(受管束 / 安全放行(依据哪次刷新) / 未核实(为什么))以及站点侧值与本地实时值的对照。
> ⏭ 只剩**真机走查**(M0 四项实测 + 装扩展后跑一轮真实取数)。真机验证前请把结果当参考。
> 配置字段与「接第二个站点」步骤见 [配置参考 · HR 在线核实](docs/configuration.md#hr-在线核实hr_check)。

### 👯 辅种管理(种子分组)

- 将指向**相同文件列表**的种子(辅种)自动归为一组,并检查组内文件大小一致性(不一致 → 警告 + 整组暂停)
- **缺文件保护**:文件丢失立即发现,整组暂停 + 打上 `MISSING` 标签,杜绝误传垃圾数据
- 下载冲突(多种子写同一份文件)立即拦截,整组暂停 🚧

### 🐢 限速

- tracker 配置内置单种限速字段,种子添加即生效
- 规则动作支持单种上传 / 下载限速
- **全局限速曲线**:根据每天 / 每N天 / 每月的上传下载总量自动调整总限速(见[配置参考 · 全局限速曲线](docs/configuration.md#全局限速曲线))

### 🌐 Web UI

浏览器打开就是一个为 PT 而生的管理台:

- **辅种分组,一目了然** - 指向同一份文件的种子自动归成一组,组内速度、进度、站点谁在拖后腿,一眼看清
- **追剧视图,自动归位** - 全部种子按 剧 → 季 → 集 聚合,缺哪集直接标出来
- **配置全程点选** - 站点 / 规则 / 限速曲线在浏览器里完成,保存即校验、备份、热生效,不用手写 YAML
- **批量操作利落** - 搜索 + 标签 / 站点 / H&R 多维筛选,多选后批量 开始 / 暂停 / 强制汇报 / 删除;删除前先强制汇报,tracker 确认失败不删
- **出错不抓瞎** - 种子出错直接显示 tracker 报错原文(如 torrent not registered with this tracker),不用翻日志
- **双皮肤** - 经典深色仪表盘与工程仪器风一键互切,后者内置 5 套主题

连接与密钥配置见[快速开始](#快速开始)。

### 🛡️ 稳定运行

- 各功能独立计时、互不干扰,单个功能出错不影响其他功能与整体运行
- 运行状态与历史记录都保存在数据目录,重启接着上次继续;状态定期落盘,断电 / 强杀也只丢几分钟进度
- 启动时**全量检查配置**,所有问题一次报出,不带病运行
- 同一份配置同时只能运行一个实例,防止两个实例互相打架
- **主动通知**(见 [桌面通知](#桌面通知)):出错 / 危险情况推送系统原生通知,免打扰时段 + 频率上限防打扰
- **托盘常驻**(`--tray`,见 [托盘模式](#托盘模式)):缩进系统托盘,随时暂停 / 恢复自动管理、开关通知、设开机自启;重复启动自动唤起已运行的窗口
- 从已有种子的 tracker 一键导出 YAML 配置模板

### 🧩 自定义规则引擎 🚧

- 触发时机 + 筛选条件 + 动作,动作顺序执行,支持去重、冷却与错误处理(见[配置参考 · 规则系统](docs/configuration.md#规则系统))
- **16 种条件 × 12 种动作**,全部可自由组合;除固定间隔外还支持种子添加 / 状态变化 / 删除事件触发

## 快速开始

### 环境要求

- Python 3.12+
- [uv](https://docs.astral.sh/uv/) 0.12+(依赖与虚拟环境管理;Windows: `winget install astral-sh.uv`)
- qBittorrent(已开启 Web UI;已在 qB 5.2.3 测试,其他版本待测)

### 1. 安装

```text
# 下载 / 克隆源码后,进入项目目录
commands run env.sync    # 一次装齐全部依赖(含托盘 / Web UI / 测试)
```

> `commands run <task>` 是本仓库命令的统一调用面: `<task>` 对应 `.commands/` 包里的一条命令
> (真实命令与环境前缀都在包里, 不在这里抄)。不知道有哪些就 `list` 逐级看 —— 用法见 AGENTS.md。

依赖全部锁定在锁文件里,重复执行 `commands run env.sync` 结果可复现,无需其他安装步骤。

### 2. 配置

**推荐用 Web UI 图形化配置**:`minimal.yml` 已默认开启 Web UI,开箱只需要填好 qBittorrent 连接信息,其余全部在浏览器里点选完成:

```yaml
config:
    qbittorrent:          # qBittorrent 连接信息(WebUI 端口)
        host: 127.0.0.1
        port: 8080
        username: <USERNAME>
        password: <PASSWORD>
    web:                  # 本程序 Web UI(minimal.yml 已默认开启)
        enabled: true
        port: 8080        # 与 qB WebUI 同端口时请改开一个
        token: ""         # 访问密钥; 留空 = 首次启动随机生成
```

启动程序(见下一步)后,浏览器打开 `http://127.0.0.1:8080`,输入访问密钥登录,在「设置」页完成站点 / 规则 / 限速曲线等**全部配置**:表单化增删改、保存前自动校验、自动 `.bak` 备份、改完即时生效。

> 访问密钥留空时首次启动自动生成(存到数据目录的 `web.token` 文件,日志打印一次);本机访问可开启「跳过本地验证」免密钥进入。
>
> ⚠️ **安全提示**:默认只监听 `127.0.0.1`(仅本机可访问)。改为 `0.0.0.0` 等对外地址会把**可删除种子、可改配置**的管理界面暴露到网络,请务必配合防火墙 / 反向代理 / 额外鉴权使用。

偏好直接编辑 YAML 时,也可以根据**已有种子的 tracker 自动导出配置模板**(先在 `minimal.yml` 填好连接信息):

```bash
# 读取 minimal.yml 的连接信息, 把生成的模板写入 config.yml
# ⚠️ 注意: 该命令会写入(覆盖)指定的输出文件
uv run auto-qb minimal.yml --export-yaml config.yml

# 加 --only-missing 只导出尚未配置的 tracker, 生成最小骨架(同样会覆盖输出文件)
uv run auto-qb minimal.yml --export-yaml config.yml --only-missing
```

> 全部配置项的字段说明与完整示例见[配置参考](docs/configuration.md);直接编辑 YAML 后需重启程序生效。

### 3. 运行

```bash
# 强烈建议第一次先试运行: 只打印将执行的动作,不实际改动 qBittorrent
uv run auto-qb --dry-run

# 确认无误后正式运行(默认读取 config.yml)
uv run auto-qb

# 指定配置文件
uv run auto-qb my-config.yml

# 托盘常驻模式(状态窗口 + 系统托盘, 详见[托盘模式](#托盘模式))
uv run auto-qb --tray
```

> 上面是用户可见的入口; `commands run dev.run -- config.yml` 走的是同一份源码入口,两者等价。

## 命令行参数

| 参数                         | 说明                                                                    |
|------------------------------|-------------------------------------------------------------------------|
| `config`                     | 配置文件路径(位置参数,默认 `config.yml`)                              |
| `--export-yaml`, `-e OUTPUT` | 导出 YAML 配置模板到 OUTPUT 后退出(不进入自动管理)                     |
| `--only-missing`             | 仅导出尚未配置的 tracker 站点(配合 `--export-yaml` 使用)               |
| `--dry-run`, `-n`            | 试运行:只打印将执行的动作,不实际操作 qBittorrent                      |
| `--tray`                     | 托盘常驻模式:系统托盘图标 + 状态窗口;再次启动会唤起已运行实例的窗口   |
| `--export-torrents_info`     | 把全部种子的信息导出到 `torrents.txt`(排障用,导出后退出)             |

## 托盘模式

不想留终端窗口时,可用 `--tray` 让程序缩进系统托盘常驻,自动化照常在后台运行:

```bash
uv run auto-qb --tray             # 默认 config.yml, 也可在前面指定配置文件
uv run auto-qb --tray --dry-run   # 托盘模式同样支持试运行
```

- **深色状态窗口**:显示受管种子数、运行时长、qB 连接状态与最近日志;点关闭按钮只是隐藏到托盘,程序继续在后台运行
- **托盘菜单 / 窗口控件**:暂停 / 恢复自动管理(暂停期间不动任何种子,恢复后自动补上错过的处理)、通知开关(随时生效)、开机自启开关、打开 qBittorrent WebUI、打开日志目录、退出
- **不会双开**:重复执行 `--tray` 时,第二次启动会唤起已运行实例的窗口,然后自己退出
- **开机自启**:注册为当前用户的开机启动项(自动带上当前配置文件路径),不需要时在菜单中关闭即可
- 无桌面环境(SSH / 服务会话)下托盘模式会报错退出,不影响命令行模式

## 桌面通知

配置 `notify.enabled: true` 后,运行中的 WARNING / ERROR 日志(缺文件、下载冲突、跳检失败等)会以**系统原生通知**推送,无需安装任何额外组件:

- **防打扰**:免打扰时段(支持跨午夜)、每小时条数上限、相同内容自动去重
- 全屏游戏 / 演示等繁忙场景由系统专注助手 / 勿扰模式自动静默
- 托盘菜单可随时开关通知(本次运行生效,重启后回到配置状态)
- `--dry-run` 试运行不发送通知

## 配置参考

全部配置项的字段说明已整理到独立文档 **[配置参考](docs/configuration.md)**,内容包括:

- 两种配置方式的对比(Web UI 推荐 / YAML 直改需重启)与通用约定(单位、正则、路径、多实例数据目录)
- 完整 YAML 配置示例(每个字段的注释说明)
- 全局限速曲线:周期 / 阈值 / 限速档位的写法与 Traffic Monitor 数据来源接入
- 规则系统:触发时机、16 种筛选条件、12 种动作、去重与错误处理、状态映射表、checking 校验 / 跳检的风险对照与安全保障

最常踩的几个点提前说:时间单位 `S/M/H/D`、速度 `[KMG]iB/s`、大小 `[KMGT]iB`(不区分大小写);标签 / 路径匹配用 `regex:` 前缀走正则;多实例运行请为每个实例单独指定 `data_dir`。

## 设计理念

这些原则决定了 auto-qb 在各种情况下的行为方式,也是它「敢于自动操作」的底气:

- **幂等**:同一个动作重复执行不会产生额外副作用;「每天一次」这类节奏由程序自己记录、重启也不受影响,而不是靠检查得频繁来碰运气
- **保守默认**:高风险动作(跳检、强制汇报、删除种子、覆盖限速)默认关闭,或只对你明确允许的范围生效
- **状态可续**:跨重启的状态(规则执行历史、上传量统计、限速状态等)都保存在数据目录,重启后接着上次的进度继续
- **先查再跑**:启动时把整个配置检查一遍,所有问题一次报出来;检查通过才开始操作 qBittorrent

> 想了解代码结构与内部实现?开发者请看 [memory-bank/README.md](memory-bank/README.md) 知识库。

## 开发测试

想参与开发或验证行为?测试全部使用替身,不连真实 qBittorrent,可随时全量运行:

```text
commands run env.sync                              # 同步依赖(含开发/测试依赖)

commands run test.full                             # 全量测试
commands run test.quick                            # 快速迭代时跳过覆盖率报表
commands run test.one -- tests/test_grouping.py    # 只跑单个测试文件
```

## 免责声明与许可证

本项目采用 [Apache License 2.0](https://www.apache.org/licenses/LICENSE-2.0)。

本项目仅供个人学习与交流使用。使用本工具产生的一切后果由使用者自行承担,请务必:

- 遵守各 PT 站点的规则,谨慎使用跳检、强制汇报等高风险功能
- 上线前先用 `--dry-run` 试运行确认行为符合预期
- 作者不对因使用本工具导致的账号处罚、数据损失等负责

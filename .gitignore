__pycache__/
test.yml
torrents.txt
example-torrent-info.txt
test.py

# Python虚拟环境
.venv/

# 运行时数据目录(2026-09-09: 状态/单实例锁/日志/跳检备份集中到 auto-qb-data/)
auto-qb-data/

# Docker 部署的配置目录(docs/deployment.md 快速开始: cp docker/config.example.yml config/config.yml
# 后填真实 qB 凭据) —— 凭据不入库, 与根 config.yml 同一红线口径
config/

# 测试跳检备份残留(state_file 配在仓库根时的兜底)
skip-check-backup/

# pytest-cov覆盖率文件
.coverage
.coverage.*
htmlcov/
cov-report.txt

# 程序运行日志(旧默认目录; 现默认已迁 auto-qb-data/, 此条兜底历史 config.yml 的 log.file)
logs/

# ZCode相关文件
.zcode/

# Trae相关文件
.trae/

# Awesome-Copilot 列表文件
awesome-copilot-llms.txt

# APM dependencies
apm_modules/

# Python 打包产物 (uv build / hatchling)
dist/

# AutoClaw相关文件
.openclaw/
.openclaw-attachments/
.cluster/
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md

# WorkBuddy AI 相关文件(会话记忆/技能数据目录)
.workbuddy-ai/

# 内置 CLI (codebuddy.js) 的项目级技能扫描目录; 由 scripts/sync_agent_skills.py 生成链接
.codebuddy/

# ?
.mimosa/

# commands wrapper: 由 commands skill 的 install_wrapper.py 按平台生成(Windows 多一份 .cmd),
# 生成物不入库 —— 单点定义在生成器里, 入库就变成第 N+1 处副本
/commands
/commands.cmd
